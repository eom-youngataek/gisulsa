#### **시스템 프로그래밍 핵심: 메모리 예외 처리**

---

#### 답안 전체 스토리 흐름 (목차)

```
Ⅰ. 개요 (왜 메모리 예외가 시스템 안정성의 핵심인가)
Ⅱ. 메모리 예외 유형 및 원인
Ⅲ. 언어별 예외 처리 메커니즘
Ⅳ. 결론
```

포인트: 개요에서 **"앞서 다룬 ARIES 회복 알고리즘이 'DB 장애 후 로그로 복구'한다면, 메모리 예외 처리는 '프로그램 실행 중 메모리 접근 오류를 탐지·처리해 시스템 전체 붕괴를 방지하는 안전망'이다 — Buffer Overflow·Null Pointer Dereference·Stack Overflow 같은 메모리 예외는 앞서 다룬 시큐어코딩의 핵심 취약점이자 인포스틸러·랜섬웨어 등 사이버 공격의 주요 익스플로잇 경로이며, C/C++의 수동 메모리 관리와 Java·Rust의 자동·안전 메모리 관리 방식이 예외 처리 설계의 근본 차이를 만든다"**는 한 줄로 시작하면 전체 맥락이 드러납니다.

---

#### Ⅱ. 메모리 예외 유형 및 원인

**가. 5대 메모리 예외 유형**

|예외 유형|정의|발생 원인|보안 영향|
|---|---|---|---|
|**Buffer Overflow**|할당된 버퍼 크기 초과 데이터 쓰기|입력 길이 미검증·배열 경계 초과|스택/힙 오염·코드 실행 🚨|
|**Null Pointer Dereference**|NULL 포인터 역참조|초기화 전 포인터 사용·해제 후 접근|프로세스 강제 종료(Segfault)|
|**Stack Overflow**|스택 메모리 한계 초과|무한 재귀·과도한 지역 변수|프로세스 종료·취약점 악용|
|**Use After Free**|해제된 메모리 재접근|free() 후 포인터 계속 사용|힙 오염·코드 실행 🚨|
|**Memory Leak**|할당 메모리 미해제 누적|free() 누락·참조 순환|장기 운영 시 OOM Killer 발동|

---

**나. 메모리 예외 핵심 비교**

| **핵심 척도**                 | **📊 발생 원인·구조 🚨**                                                | **🔑 탐지·디버깅 🚨**                                               | **🏁 방어·예방 💯**                                                                         |
| ------------------------- | ----------------------------------------------------------------- | -------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| **Buffer Overflow**       | 스택: 반환 주소 덮어쓰기 / 힙: 청크 헤더 오염 / 경계 검사 누락이 근본 원인                    | Valgrind·AddressSanitizer(ASan) / 스택 카나리(Stack Canary) 값 변조 감지 | **경계 검사 필수**: strncpy·snprintf 사용 / ASLR·DEP(NX bit) OS 보호 / 앞서 다룬 **SAST로 정적 탐지**      |
| **Null Pointer / UAF**    | 포인터 초기화 없이 사용 / free() 후 포인터 NULL 미설정 / 댕글링 포인터(Dangling Pointer) | GDB·ASan·Valgrind Memcheck / Core Dump 분석                      | **초기화 필수·해제 후 NULL 설정** / 스마트 포인터(C++ unique_ptr·shared_ptr) / Rust 소유권 시스템으로 컴파일 타임 차단 |
| **Stack Overflow / Leak** | 재귀 탈출 조건 누락·큰 지역 변수 / 할당-해제 쌍 불일치                                 | ulimit·스택 크기 제한 / Valgrind Massif·JVM Heap Dump                | **재귀→반복 전환** / RAII 패턴(소멸자 자동 해제) / GC(Java·Python)·Arc(Rust)로 자동 관리                    |

---

#### Ⅲ. 언어별 예외 처리 메커니즘

**가. 언어별 메모리 안전성 비교**

```
[언어별 메모리 관리 방식]

C (수동 관리):
  malloc() → 직접 사용 → free()
  → 개발자 책임 전담
  → Buffer Overflow·UAF·Leak 모두 발생 가능 🚨

C++ (반자동):
  RAII: 스코프 종료 시 소멸자 자동 호출
  스마트 포인터: unique_ptr(단독)·shared_ptr(공유)
  → 수동 delete 없이 자동 해제 ✅
  → 그러나 raw pointer 혼용 시 취약

Java (GC 기반):
  GC(Garbage Collector)가 미참조 객체 자동 해제
  NullPointerException → try-catch 처리
  → Buffer Overflow 원천 차단 ✅
  → GC Stop-the-World 지연 단점

Rust (소유권 시스템):
  컴파일 타임 소유권·수명 검사
  → Buffer Overflow·UAF·Null 역참조
    런타임 전에 컴파일 오류로 차단 ✅
  → 앞서 다룬 임베디드 AI·시스템 SW 차세대 표준
```

---

**나. 실무 예외 처리 코드 패턴**

c

```c
/* C: 안전한 메모리 처리 패턴 */
char *buf = malloc(SIZE);
if (buf == NULL) {           /* 할당 실패 체크 필수 */
    handle_error();
    return -1;
}
strncpy(buf, input, SIZE-1); /* 경계 검사 */
buf[SIZE-1] = '\0';          /* NULL 종료 보장 */
/* ... 사용 ... */
free(buf);
buf = NULL;                  /* 해제 후 NULL 설정 */
```

java

```java
// Java: NullPointerException 방어 패턴
try {
    String result = obj.getData();          // NPE 가능
    Objects.requireNonNull(result, "null"); // 명시적 체크
} catch (NullPointerException e) {
    logger.error("Null 접근", e);
    return Optional.empty();                // Optional 활용
}
```

rust

```rust
// Rust: 컴파일 타임 메모리 안전 보장
fn safe_access(data: &[u8], index: usize) -> Option<u8> {
    data.get(index).copied()  // 경계 초과 시 None 반환
}                             // panic 없이 안전 처리
```

---

**다. 메모리 예외와 보안 연계**

```
[메모리 예외 → 보안 공격 경로]

Buffer Overflow 취약점
       ↓
공격자: 반환 주소 덮어쓰기
       ↓
임의 코드 실행 (셸코드 주입)
       ↓
앞서 다룬 인포스틸러·랜섬웨어 설치 🚨

방어 계층:
①코드 레벨: 경계 검사·안전 함수
②컴파일 레벨: 스택 카나리·ASan
③OS 레벨: ASLR·DEP(NX bit)
④런타임 레벨: 샌드박스·컨테이너 격리
앞서 다룬 DevSecOps Shift-Left 연계
```

---

**(제언)** "메모리 예외 처리는 '기능 구현'이 아닌 '시스템 신뢰성과 보안'의 문제입니다. **앞서 다룬 시큐어코딩 가이드의 핵심 취약점(Buffer Overflow·UAF)이 메모리 예외에서 비롯되므로, C/C++ 레거시 코드는 SAST·Valgrind·ASan으로 정적·동적 탐지를 병행하고, 신규 시스템 소프트웨어는 컴파일 타임 메모리 안전성을 보장하는 Rust 도입을 적극 검토하며, 앞서 다룬 AIDLC 에이전틱 코딩 환경에서는 AI 생성 C 코드의 메모리 안전성을 반드시 SAST 품질 게이트로 자동 검증하는 것이 핵심입니다.**"
