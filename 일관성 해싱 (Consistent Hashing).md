### **일관성 해싱 (Consistent Hashing)**

#### 답안 전체 스토리 흐름 (목차)

```
Ⅰ. 개요 (왜 일반 해싱으로는 분산 환경을 감당 못 하는가)
Ⅱ. 일관성 해싱 핵심 원리
Ⅲ. 가상 노드 및 실무 적용
Ⅳ. 결론
```

포인트: 개요에서 \*\*"앞서 다룬 CAP 이론·BASE의 분산 DB 환경에서 '데이터를 어느 노드에 저장할 것인가'를 결정하는 전통적 모듈러 해싱(key % N)은 노드 추가·제거 시 N이 변해 거의 모든 키의 저장 노드가 바뀌는 대규모 데이터 재배치(Reshuffling)가 발생하는 치명적 한계가 있다 — 일관성 해싱(Consistent Hashing)은 '해시 공간을 원형 링으로 구성해 노드 추가·제거 시 영향받는 키를 K/N개로 최소화'하는 알고리즘으로, 앞서 다룬 Cassandra·DynamoDB·Redis Cluster·CDN의 핵심 데이터 분산 기반이며 분산 캐시·로드밸런서·분산 스토리지 패브릭의 핵심 라우팅 알고리즘"\*\*이라는 한 줄로 시작하면 전체 맥락이 드러납니다.

***

#### Ⅱ. 일관성 해싱 핵심 원리

**가. 전통 해싱 vs 일관성 해싱**

```
[전통 모듈러 해싱의 한계]

서버 3대: key % 3
  key=100 → 100%3=1 → 서버1
  key=200 → 200%3=2 → 서버2

서버 1대 추가 (총 4대): key % 4
  key=100 → 100%4=0 → 서버0  (변경!) 🚨
  key=200 → 200%4=0 → 서버0  (변경!) 🚨
→ 노드 변화 시 대부분 키 재배치 → 캐시 무효화 대란 🚨

[일관성 해싱]
해시 공간: 0 ~ 2^32-1 → 원형 링(Ring)으로 구성
노드와 키 모두 동일 해시 함수로 링에 배치
→ 키는 링에서 시계 방향 첫 번째 노드에 저장
→ 노드 추가·제거 시 인접 노드 키만 영향 ✅
```

***

**나. 일관성 해싱 핵심 원리**

| **핵심 척도** | **📊 링 구조·배치 🚨**                                                                                                | **🔑 노드 추가·제거 🚨**                                                                                                      | **🏁 가상 노드(Virtual Node) 💯**                                                                                     |
| :-------- | :--------------------------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------------- |
| **핵심 원리** | **해시 링(Hash Ring)**: 0~2³²-1 원형 공간 / 노드를 해시값으로 링에 배치 / 키를 해시해 링에서 시계방향 첫 노드에 저장 / **O(log N)**: 이진 탐색으로 담당 노드 탐색 | **노드 추가**: 새 노드의 해시값 위치에 삽입 / 새 노드와 전임 노드 사이 키만 재배치 / 전체 키의 K/N개만 이동 ✅ / **노드 제거**: 해당 노드 키를 다음 노드로 이전 / 나머지 노드 영향 없음 ✅ | **불균형 문제**: 노드가 적으면 링에서 편중 배치 / 특정 노드 과부하 🚨 / **해결**: 노드당 V개 가상 노드 생성 / 각 가상 노드 링에 분산 배치 / 부하 균등 분산 ✅            |
| **수식·성능** | **재배치 키 수**: K/N개 (K=전체키·N=노드수) / 일반 해싱: 대부분 키 재배치 vs 일관성: K/N만 재배치 / **조회 복잡도**: O(log N) (정렬된 링 이진 탐색)         | **예시**: 노드 3개·키 300개 → 노드 1개 추가 시 / 일반 해싱: \~225개 재배치 🚨 / 일관성 해싱: \~75개만 재배치 ✅                                         | **가상 노드 수(V)**: V=100~200 일반 설정 / V 클수록 균등 분산↑·메모리↑ / **Cassandra**: Vnodes 기본 256개 / 앞서 다룬 **DynamoDB** 핵심 구현 기반 |
| **실무 구현** | **데이터 구조**: 정렬된 배열·TreeMap으로 링 구현 / Java: TreeMap.ceilingKey() / Python: bisect 모듈 / 해시 함수: MD5·SHA-1·MurmurHash | **복제 전략 연계**: 앞서 다룬 **BASE의 N개 복제** / 일관성 해싱으로 복제 노드 결정 / Cassandra: 시계 방향 N번째 노드에 복제                                   | **핫스팟 방지**: 인기 키 분산 / 데이터 마이그레이션 최소화 / 앞서 다룬 **분산 스토리지 패브릭** 데이터 배치 최적화                                           |

***

#### Ⅲ. 가상 노드 및 실무 적용

**가. 도식화**

```
[일관성 해싱 링 구조]

        0
    ┌───┴───┐
2³¹─┤       ├─ 서버A (hash=100)
    │  링   │
    │       │  ← key=150 → 시계방향 첫 노드 = 서버B
서버C─┤       ├─ 서버B (hash=200)
(hash=350)  │
    └───┬───┘
       2³²-1

[노드 추가 시 재배치]
기존: 서버A(100)·서버B(200)·서버C(350)
      key=150 → 서버B
      key=250 → 서버C

서버D(300) 추가:
      key=150 → 서버B (변경 없음 ✅)
      key=250 → 서버D (서버C→서버D 변경, 이 구간만) ✅

[가상 노드로 부하 균등화]
서버A → A-1(50), A-2(180), A-3(320) 가상노드 3개
서버B → B-1(120), B-2(250), B-3(400) 가상노드 3개
→ 링에 6개 노드 분산 배치 → 균등 부하 ✅
```

***

**나. 실무 적용 사례**

| 시스템                  | 적용 방식             | 핵심                    |
| :------------------- | :---------------- | :-------------------- |
| **Amazon DynamoDB**  | 일관성 해싱 기반 파티션 분산  | 앞서 다룬 BASE·AP 선택      |
| **Apache Cassandra** | Vnodes 256개 기본 설정 | 노드 추가 시 자동 재균형        |
| **Redis Cluster**    | 16,384개 해시 슬롯 분산  | CRC16(key)%16384      |
| **CDN (Akamai)**     | 콘텐츠→엣지 서버 일관성 해싱  | 캐시 히트율 최대화            |
| **Nginx 업스트림**       | 일관성 해싱 로드밸런싱      | 세션 고정(Sticky Session) |

***

**다. 앞서 다룬 개념과의 연결**

| 연계 개념           | 연결 내용                            |
| :-------------- | :------------------------------- |
| **CAP·BASE**    | AP 선택 분산 DB의 데이터 파티셔닝 기반 알고리즘    |
| **분산 스토리지 패브릭** | Ceph CRUSH 알고리즘 = 일관성 해싱 확장 변형   |
| **MAB 탐색·활용**   | 가상 노드 수(V) = 균등성·비용 트레이드오프       |
| **Raft 합의**     | 파티션 리더 선출에 일관성 해싱으로 파티션 담당 노드 결정 |

***

**(제언)** "일관성 해싱은 '분산 시스템의 수평 확장을 노드 변화 충격 없이 가능하게 하는 핵심 알고리즘'입니다. **앞서 다룬 Cassandra·DynamoDB 같은 AP 분산 DB가 노드를 탄력적으로 추가·제거하면서도 서비스를 중단하지 않는 것이 일관성 해싱+가상 노드의 결합 덕분이며, 앞서 다룬 분산 스토리지 패브릭의 Ceph CRUSH 알고리즘도 일관성 해싱의 확장 형태로 오브젝트를 OSD에 분산 배치하고, CDN·로드밸런서에서 세션 고정과 캐시 히트율 극대화를 동시에 달성하는 것이 일관성 해싱의 실무 핵심 가치입니다.**"
