### **InnoDB 데이터 무결성 핵심: 이중 쓰기 버퍼 (Doublewrite Buffer)**

***

#### 답안 전체 스토리 흐름 (목차)

```
Ⅰ. 개요 (왜 WAL만으로는 부분 페이지 쓰기를 못 막는가)
Ⅱ. 이중 쓰기 버퍼 핵심 원리
Ⅲ. 동작 단계 및 성능 트레이드오프
Ⅳ. 결론
```

포인트: 개요에서 \*\*"앞서 다룬 ARIES·WAL이 '로그 먼저 쓰고 페이지 나중에 디스크에 기록해 장애 시 복구'하는 원칙이라면, 이중 쓰기 버퍼(Doublewrite Buffer)는 WAL이 해결 못하는 '부분 페이지 쓰기(Partial Page Write) 문제'를 해결하는 InnoDB 전용 메커니즘이다 — RDBMS 페이지(16KB)와 OS 파일시스템 블록(4KB·512B)의 크기 불일치로 페이지 쓰기 도중 전원 차단 시 절반만 기록된 손상 페이지(Torn Page)가 발생하고, WAL의 Redo 로그는 '온전한 Before Image'를 전제로 하므로 손상 페이지에 Redo를 적용하면 오히려 데이터가 더 깨지는 치명적 문제를 이중 쓰기 버퍼가 원본 페이지 백업본으로 해소"\*\*라는 한 줄로 시작하면 전체 맥락이 드러납니다.

***

#### Ⅱ. 이중 쓰기 버퍼 핵심 원리

**가. 부분 페이지 쓰기 문제**

```
[Torn Page (손상 페이지) 발생 구조]

InnoDB 페이지: 16KB
OS 블록:        4KB (4번에 나눠 쓰기)

정상 쓰기:
  [블록1 4KB] [블록2 4KB] [블록3 4KB] [블록4 4KB]
  모두 성공 → 16KB 페이지 완성 ✅

부분 쓰기 (2번째 블록 중 전원 차단):
  [블록1 새값] [블록2 ??손상??] [블록3 구값] [블록4 구값]
  → Torn Page 발생 🚨

WAL Redo 적용 시:
  손상 페이지에 Redo 로그 적용
  → Before Image 자체가 오염
  → 복구 불가·데이터 영구 손실 🚨
```

***

**나. 이중 쓰기 버퍼 핵심 동작**

| **핵심 척도**  | **📊 1단계: 이중 쓰기 영역 쓰기 🚨**                                                                                  | **🔑 2단계: 실제 데이터 파일 쓰기 🚨**                                                                                     | **🏁 장애 복구 활용 💯**                                                                                                               |
| :--------- | :---------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------- |
| **핵심 동작**  | **버퍼 풀 Dirty Page** 선별 / 실제 데이터 파일 쓰기 전 / **이중 쓰기 영역(ibdata1 내 2MB 연속 공간)에 먼저 순차 쓰기** / 연속 순차 I/O → 빠른 쓰기 ✅ | 이중 쓰기 영역 쓰기 완료 확인 후 / **실제 테이블스페이스(.ibd 파일)에 랜덤 I/O로 기록** / 이 단계에서 전원 차단 → Torn Page 가능                         | **InnoDB 시작 시 자동 검사**: 이중 쓰기 영역 백업본 vs 실제 페이지 비교 / **Torn Page 탐지**: 체크섬 불일치로 탐지 / **백업본으로 복원 후** WAL Redo 적용 → 정상 복구 ✅          |
| **데이터 구조** | **이중 쓰기 영역**: ibdata1 시스템 테이블스페이스 내 2MB / 128개 페이지(16KB×128=2MB) / **MySQL 8.0.20+**: 별도 doublewrite 파일로 분리 | **순서 보장**: 1단계 완료 fsync 후 2단계 시작 / 두 단계 모두 완료되면 이중 쓰기 영역 재사용 / 앞서 다룬 **WAL fsync**와 동일 순서 보장 원칙                 | **체크섬 검증**: innodb\_checksum\_algorithm / CRC32·FULL\_CRC32 / 페이지 헤더·트레일러 체크섬 비교 / 불일치 = Torn Page 판정                            |
| **성능 영향**  | **쓰기 2회**: 동일 데이터를 이중 쓰기 영역+실제 파일 2번 기록 / **I/O 증가**: 약 5\~10% 추가 쓰기 오버헤드 / 이중 쓰기 영역은 순차 I/O → HDD에서 영향 최소화 | **SSD 환경**: 랜덤/순차 차이 없음 → 오버헤드 감소 / **MySQL 8.0**: doublewrite 병렬화로 성능 개선 / innodb\_doublewrite\_batch\_size 조정 | **비활성화 가능**: innodb\_doublewrite=OFF / 원자적 쓰기(Atomic Write) 지원 스토리지 사용 시 / ZFS·FusionIO·AWS EBS 일부 / 앞서 다룬 **NVMe SSD 원자 쓰기** 기능 |

***

#### Ⅲ. 동작 단계 및 성능 트레이드오프

**가. 전체 동작 흐름 도식화**

```
[이중 쓰기 버퍼 전체 동작]

버퍼 풀 (Buffer Pool)
  Dirty Page 128개 선별
       ↓
━━━━━━━━━━━━━━━━━━━━━
1단계: 이중 쓰기 영역 쓰기
━━━━━━━━━━━━━━━━━━━━━
  ibdata1 이중 쓰기 영역 (2MB)
  순차 쓰기 (빠름) → fsync 완료 확인
       ↓
━━━━━━━━━━━━━━━━━━━━━
2단계: 실제 데이터 파일 쓰기
━━━━━━━━━━━━━━━━━━━━━
  .ibd 테이블스페이스 파일
  랜덤 쓰기 (페이지 위치별)
       ↓
  ✅ 정상: 양쪽 쓰기 완료
  🚨 장애: 2단계 중 전원 차단

━━━━━━━━━━━━━━━━━━━━━
장애 복구 (InnoDB 재시작 시)
━━━━━━━━━━━━━━━━━━━━━
이중 쓰기 영역 백업본
  + 체크섬 검증
       ↓
Torn Page 탐지 → 백업본으로 복원
       ↓
WAL Redo 로그 적용
  (온전한 페이지에 적용 → 정상 복구 ✅)
```

***

**나. 앞서 다룬 개념과의 연결**

| 연계 개념              | 연결 내용                                            |
| :----------------- | :----------------------------------------------- |
| **ARIES·WAL**      | WAL Redo가 온전한 페이지 전제 / 이중 쓰기 버퍼가 온전한 페이지 보장      |
| **STEAL·No-Force** | Dirty Page 비동기 디스크 기록 → Torn Page 위험 → 이중 쓰기로 해소 |
| **버퍼 관리 정책**       | 이중 쓰기 영역 = 페이지 쓰기 전 안전망                          |
| **체크섬**            | CRC32 기반 페이지 무결성 검증 = 이중 쓰기와 결합                  |
| **NVMe·SSD 원자 쓰기** | 하드웨어 원자 쓰기 지원 시 이중 쓰기 비활성화 가능                    |

***

**(제언)** "이중 쓰기 버퍼는 'WAL이 해결 못하는 마지막 1%의 페이지 무결성 위협을 물리적 백업본으로 해소하는 InnoDB의 숨은 안전망'입니다. **앞서 다룬 ARIES·WAL·STEAL·No-Force 조합이 트랜잭션 무결성을 보장한다면, 이중 쓰기 버퍼는 그 보장의 전제인 온전한 페이지를 하드웨어 장애 상황에서도 유지하는 최후 방어선이며, NVMe SSD의 원자적 쓰기(Atomic Write) 기능이 보편화되면 이중 쓰기 버퍼를 비활성화해 5\~10% I/O 오버헤드를 제거하는 것이 차세대 DB 스토리지 최적화의 핵심 방향입니다.**"
