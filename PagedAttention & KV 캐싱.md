### **LLM 추론 서빙의 혁신: PagedAttention & KV 캐싱 (vLLM)**

***

#### 답안 전체 스토리 흐름 (목차)

```
Ⅰ. 개요 (왜 LLM 추론 서버가 GPU 메모리를 낭비하는가)
Ⅱ. KV 캐싱 기본 원리 및 문제점
Ⅲ. PagedAttention 핵심 원리 및 적용 체계
Ⅳ. 결론
```

포인트: 개요에서 \*\*"앞서 다룬 QLoRA가 'LLM 학습(파인튜닝) 단계의 메모리 효율화'를 다뤘다면, PagedAttention은 'LLM 추론(서빙) 단계의 메모리 효율화'를 다루는 기술이다 — 자기회귀(Autoregressive) 생성 방식의 LLM은 매 토큰 생성 시마다 이전 모든 토큰의 Key·Value를 재계산하지 않기 위해 KV 캐시를 GPU 메모리에 유지하는데, 기존 서빙 프레임워크는 이 캐시를 요청마다 연속된(Contiguous) 메모리 블록으로 미리 최대 길이만큼 예약해 실제 사용률이 20\~40%에 불과한 메모리 낭비(Fragmentation)를 초래했으며, 2023년 UC버클리 연구팀이 vLLM과 함께 제안한 PagedAttention은 운영체제의 가상 메모리 페이징 기법을 KV 캐시 관리에 그대로 적용해 이 문제를 근본적으로 해결한 것"\*\*이라는 한 줄로 시작하면 전체 맥락이 드러납니다.

***

#### Ⅱ. KV 캐싱 기본 원리 및 문제점

**가. KV 캐싱이 필요한 이유**

```
[Transformer 자기회귀 생성과 KV 캐시]

토큰 생성 과정 (Attention 연산):
  각 새 토큰 생성 시 이전 모든 토큰의
  Key·Value 벡터와 Attention 연산 필요

캐시 없을 때:
  토큰마다 전체 시퀀스의 K·V를 매번 재계산 🚨
  → 연산량 O(n²) 반복 낭비

KV 캐시 사용:
  이전에 계산한 K·V를 GPU 메모리에 저장
  새 토큰의 K·V만 추가 계산 → 캐시에 누적
  → 연산량 대폭 감소, 대신 메모리 사용량 증가
```

**나. 기존 방식의 메모리 낭비 문제**

| 낭비 유형                              | 원인                                                     | 영향                       |
| :--------------------------------- | :----------------------------------------------------- | :----------------------- |
| **내부 단편화(Internal Fragmentation)** | 요청마다 최대 생성 길이만큼 미리 연속 메모리 예약 / 실제 생성 길이가 짧으면 나머지 공간 낭비 | GPU 메모리 실사용률 20\~40%에 불과 |
| **외부 단편화(External Fragmentation)** | 서로 다른 요청의 예약 블록 크기가 달라 메모리 중간에 사용 못하는 빈 공간 발생          | 새 요청 수용 가능 배치 크기 감소      |
| **중복 저장**                          | 병렬 샘플링(Beam Search 등)에서 공통 프리픽스도 각 시퀀스마다 별도 복사         | 동일 데이터 반복 저장으로 메모리 압박    |

***

#### Ⅲ. PagedAttention 핵심 원리 및 적용 체계

**가. PagedAttention 핵심 메커니즘**

| 개념                      | 원리                                          | OS 가상 메모리와의 대응               |
| :---------------------- | :------------------------------------------ | :--------------------------- |
| **KV 블록(Block)**        | KV 캐시를 고정 크기의 작은 블록 단위로 분할 관리               | 페이지(Page)에 대응                |
| **블록 테이블(Block Table)** | 논리적 시퀀스 순서와 물리적 GPU 메모리 블록 위치를 매핑하는 테이블     | 페이지 테이블(Page Table)에 대응      |
| **비연속 할당**              | 하나의 시퀀스가 물리적으로 흩어진 블록들을 블록 테이블로 논리적 연속처럼 참조 | 가상 메모리 주소 공간에 대응             |
| **온디맨드 할당**             | 토큰이 생성될 때마다 필요한 만큼만 블록을 새로 할당               | 페이지 폴트(Page Fault) 기반 할당에 대응 |

**나. PagedAttention이 가능하게 하는 핵심 최적화**

| 최적화 기법                         | 원리                                                                  | 효과                              |
| :----------------------------- | :------------------------------------------------------------------ | :------------------------------ |
| **메모리 공유(Copy-on-Write)**      | Beam Search·병렬 샘플링 시 공통 프리픽스 블록을 여러 시퀀스가 포인터로 공유 / 분기 시점부터만 새 블록 할당 | 중복 저장 제거로 메모리 대폭 절감             |
| **연속 배치(Continuous Batching)** | 요청이 완료되는 즉시 해제된 블록을 새 요청에 즉시 재할당 / 배치 전체가 끝날 때까지 기다리지 않음            | GPU 유휴 시간 최소화·처리량 극대화           |
| **동적 배치 크기 확장**                | 메모리 낭비가 없으므로 동일 GPU에서 훨씬 많은 요청을 동시 처리 가능                            | 기존 대비 최대 24배 처리량(Throughput) 향상 |

**다. 기존 서빙 방식 vs vLLM(PagedAttention) 비교**

| 비교 항목               | 기존 방식(연속 할당)                | vLLM(PagedAttention)             |
| :------------------ | :-------------------------- | :------------------------------- |
| **메모리 할당 방식**       | 요청별 최대 길이 사전 예약(Contiguous) | **블록 단위 동적 할당(Paged)** ✅         |
| **메모리 낭비율**         | 60\~80% 낭비 🚨               | **4% 미만** ✅                      |
| **병렬 샘플링 메모리**      | 시퀀스마다 전체 복사 🚨              | **Copy-on-Write 공유** ✅           |
| **배치 처리 방식**        | 정적 배치(Static Batching)      | **연속 배치(Continuous Batching)** ✅ |
| **처리량(Throughput)** | 기준                          | **최대 24배** ✅                     |
| **구현 복잡도**          | 단순                          | 블록 테이블 관리 오버헤드 존재                |

**라. 적용 생태계 및 확장 기술**

| 항목           | 내용                                                                   |
| :----------- | :------------------------------------------------------------------- |
| **vLLM**     | PagedAttention을 최초 구현한 오픈소스 서빙 엔진 / HuggingFace 모델 즉시 서빙 지원          |
| **경쟁·후속 기술** | TensorRT-LLM(NVIDIA)·SGLang(RadixAttention으로 공유 확장)·TGI(HuggingFace) |
| **양자화 결합**   | 앞서 다룬 **QLoRA·GPTQ** 등 양자화 모델과 결합해 메모리 효율 이중 확보                      |
| **분산 서빙 확장** | 텐서 병렬화(Tensor Parallelism)와 결합해 다중 GPU 서빙으로 확장                       |

***

**(제언)** "PagedAttention은 '운영체제가 반세기 동안 검증해온 가상 메모리 페이징이라는 고전적 해법을 LLM 추론 서빙이라는 최신 문제에 그대로 이식'한 것으로, 컴퓨터 시스템의 근본 원리가 세대를 넘어 재활용되는 좋은 사례입니다. 다만 블록 테이블 관리와 온디맨드 할당에는 필연적으로 소프트웨어 오버헤드가 수반되므로, 극히 짧고 균일한 길이의 요청만 처리하는 특수 워크로드에서는 기존 연속 할당 방식이 오히려 단순하고 유리할 수 있어 워크로드 특성에 따른 벤치마크 검증이 필요하며, 실무에서는 QLoRA 등으로 모델 자체를 양자화하고 그 위에서 vLLM으로 서빙해 메모리 효율을 학습·추론 양 단계에서 동시에 확보하는 파이프라인 설계가 GPU 인프라 비용 최적화의 핵심입니다.

***

**앞서 다룬 개념과의 연결**

| 연계 개념           | 연결 내용                                                       |
| :-------------- | :---------------------------------------------------------- |
| **QLoRA**       | 학습 단계 메모리 효율화(QLoRA) + 추론 단계 메모리 효율화(PagedAttention)의 상호 보완 |
| **CXL 메모리 풀링**  | GPU 메모리 부족 시 CXL 메모리 풀을 KV 캐시 확장 공간으로 활용하는 연구 방향과 연결        |
| **AI 서비스 대가산정** | 처리량 24배 향상이 토큰당 서빙 비용을 직접 낮춰 대가산정 원가 구조에 영향                 |
| **온디바이스 NPU**   | PagedAttention은 서버급 기술이나, 메모리 효율 철학은 엣지 추론 최적화에도 응용         |
| **HBM4·CXL**    | KV 캐시 대역폭 병목 해소를 위해 고대역폭 메모리와 결합한 서빙 아키텍처 설계 필요             |
