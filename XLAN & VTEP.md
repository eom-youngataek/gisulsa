### **클라우드 네트워크 가상화 핵심: VXLAN & VTEP**

***

#### 답안 전체 스토리 흐름 (목차)

```
Ⅰ. 개요 (왜 VLAN만으로는 클라우드 규모를 감당 못 하는가)
Ⅱ. VXLAN 핵심 구조 및 동작 원리
Ⅲ. VTEP 동작 및 적용 체계
Ⅳ. 결론
```

포인트: 개요에서 \*\*"앞서 다룬 SRv6가 'WAN 레벨 소스 라우팅 혁신'이라면, VXLAN(Virtual eXtensible LAN)은 '데이터센터 내부 L2 네트워크를 L3 UDP 터널 위에 확장하는 오버레이 가상화 기술'이다 — 전통 VLAN이 4,094개 제한·물리 경계 종속·STP 비효율 문제를 갖는 반면, VXLAN은 24비트 VNI(VXLAN Network Identifier)로 1,600만↑ 가상 네트워크를 지원하고 VTEP(VXLAN Tunnel EndPoint)이 L2 프레임을 UDP 패킷으로 캡슐화해 IP 네트워크 전체로 확장하며, 앞서 다룬 AIDC 특별법 기반 AI 데이터센터·OpenStack·VMware NSX·Kubernetes CNI의 네트워크 가상화 핵심 기반이자 앞서 다룬 일관성 해싱·CXL 메모리 풀링과 함께 현대 데이터센터 패브릭을 구성하는 3대 핵심 기술"\*\*이라는 한 줄로 시작하면 전체 맥락이 드러납니다.

***

#### Ⅱ. VXLAN 핵심 구조 및 동작 원리

**가. VLAN 한계와 VXLAN 등장**

```
[VLAN vs VXLAN 한계 극복]

VLAN 한계:
  ①4,094개 제한: 12비트 VLAN ID → 대규모 멀티테넌트 불가
  ②물리 경계 종속: L2 세그먼트 = 물리 스위치 도메인
  ③STP(Spanning Tree): 루프 방지로 링크 절반 비활성 → 낭비
  ④VM 이동 제약: L2 도메인 밖으로 VM 라이브 마이그레이션 불가

VXLAN 해결:
  ①24비트 VNI: 16,777,216개 가상 네트워크 ✅
  ②L3 오버레이: IP 네트워크 어디든 L2 확장 ✅
  ③ECMP 활용: STP 없이 다중 경로 로드밸런싱 ✅
  ④VM 이동 자유: IP 연결만 있으면 L2 세그먼트 유지 ✅
```

***

**나. VXLAN 패킷 구조**

```
[VXLAN 캡슐화 구조]

원본 L2 프레임:
┌──────────────────────────────────┐
│ Ethernet Header │ IP │ TCP │ Data│ (테넌트 트래픽)
└──────────────────────────────────┘
         ↓ VTEP 캡슐화
┌─────────────────────────────────────────────────────┐
│ Outer ETH │ Outer IP │ UDP(4789) │ VXLAN │ Inner Frame│
│ (VTEP MAC)│(VTEP IP) │           │ Header│ (원본 전체)│
└─────────────────────────────────────────────────────┘

VXLAN Header (8바이트):
  ├─ Flags (8비트): I비트=1 (VNI 유효)
  ├─ Reserved (24비트)
  └─ VNI (24비트): 가상 네트워크 식별자 ← 핵심

UDP 포트: 4789 (IANA 표준)
오버헤드: 50바이트 추가 (Outer ETH+IP+UDP+VXLAN)
```

***

**나. VXLAN 핵심 체계**

| **핵심 척도**   | **📊 VNI·캡슐화 🚨**                                                                                      | **🔑 MAC 학습·플러딩 🚨**                                                                          | **🏁 언더레이·오버레이 🚨**                                                                        |
| :---------- | :----------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------- |
| **핵심 원리**   | **VNI(VXLAN Network Identifier)**: 24비트 / 테넌트 가상 네트워크 식별 / VLAN ID와 매핑 가능 / 동일 VNI = 동일 L2 세그먼트        | **멀티캐스트 플러딩**: ARP 브로드캐스트를 멀티캐스트 그룹으로 전달 / **EVPN(BGP)**: 제어 플레인으로 MAC·IP 중앙 배포 / 플러딩 없는 학습 ✅ | **언더레이**: 물리 IP 네트워크 / VTEP 간 IP 연결 / ECMP 로드밸런싱 / **오버레이**: VXLAN 가상 L2 세그먼트 / 테넌트 트래픽 격리 |
| **ECMP 연계** | 기존 VLAN: STP로 단일 경로 / **VXLAN+ECMP**: 외부 IP 헤더로 다중 경로 / UDP 소스 포트 해시로 플로우별 분산 / 앞서 다룬 **일관성 해싱** 원리 적용 | **ARP 억제**: EVPN이 MAC-IP 사전 배포 / ARP 브로드캐스트 제거 / 앞서 다룬 **GARP** VXLAN 환경 전파                   | **스파인-리프 패브릭**: 언더레이 ECMP / 앞서 다룬 **CXL 3.0** 스파인-리프와 동일 토폴로지                              |

***

#### Ⅲ. VTEP 동작 및 적용 체계

**가. VTEP 핵심 기능**

```
[VTEP (VXLAN Tunnel EndPoint) 동작]

VTEP = VXLAN 캡슐화·역캡슐화 수행 주체

송신 VTEP (캡슐화):
  ①테넌트 VM L2 프레임 수신
  ②목적지 MAC으로 원격 VTEP IP 조회
    (EVPN BGP 테이블 또는 멀티캐스트)
  ③Outer IP 헤더 추가 (src=로컬VTEP·dst=원격VTEP)
  ④UDP 4789 포트·VXLAN 헤더(VNI) 추가
  ⑤언더레이 네트워크로 전송

수신 VTEP (역캡슐화):
  ①UDP 4789 패킷 수신
  ②VXLAN 헤더 제거 → VNI 확인 → 테넌트 구분
  ③Outer 헤더 제거 → 원본 L2 프레임 복원
  ④해당 VNI의 로컬 VM에게 전달

VTEP 구현 유형:
  하드웨어 VTEP: 물리 스위치(Cisco Nexus·Arista)
  소프트웨어 VTEP: OVS(Open vSwitch)·Linux 커널 VXLAN
  하이퍼바이저 VTEP: VMware ESXi·KVM
```

***

**가. VXLAN 제어 플레인 비교**

| 방식                            | 원리                     | 장점                  | 한계              |
| :---------------------------- | :--------------------- | :------------------ | :-------------- |
| **멀티캐스트**                     | ARP를 멀티캐스트 그룹 전달       | 단순 구현               | 멀티캐스트 인프라 필요 🚨 |
| **유니캐스트·Ingress Replication** | 알려진 VTEP 목록에 유니캐스트 복제  | 멀티캐스트 불필요           | 플러딩 트래픽 증가      |
| **BGP EVPN**                  | BGP로 MAC·IP·VTEP 정보 배포 | **플러딩 제거·확장성 최고** ✅ | 구현 복잡           |

***

**나. VXLAN vs VLAN vs GRE 비교**

| 비교 항목      | VLAN      | GRE    | VXLAN                  |
| :--------- | :-------- | :----- | :--------------------- |
| **세그먼트 수** | 4,094개 🚨 | 제한 없음  | **16M↑** ✅             |
| **캡슐화**    | 802.1Q 태그 | GRE 헤더 | UDP+VXLAN 헤더           |
| **L4 포트**  | 없음        | 없음     | **UDP 4789** (ECMP 유리) |
| **ECMP**   | STP 제한 🚨 | 가능     | **최적** ✅               |
| **오버헤드**   | 4바이트      | 24바이트  | **50바이트**              |
| **MTU 요구** | 1518      | 1524↑  | **1574↑** (점보프레임 권장)   |
| **멀티테넌트**  | 제한적       | 가능     | **최적** ✅               |

***

**다. 클라우드·컨테이너 적용 체계**

| 적용 환경                 | VXLAN 활용 방식                    | 핵심 연계         |
| :-------------------- | :----------------------------- | :------------ |
| **OpenStack Neutron** | VXLAN 오버레이로 테넌트 네트워크 격리        | OVS VTEP      |
| **VMware NSX**        | NSX-T VTEP으로 VM 간 VXLAN        | 분산 VTEP       |
| **Kubernetes CNI**    | Flannel·Calico·Cilium VXLAN 모드 | Pod 네트워크 오버레이 |
| **앞서 다룬 AIDC**        | AI 클러스터 테넌트 격리·GPU 네트워크        | 스파인-리프+VXLAN  |
| **앞서 다룬 CASB**        | 테넌트별 VXLAN VNI로 트래픽 가시성 확보     | VNI 기반 정책     |

***

**라. 앞서 다룬 개념과의 연결**

| 연계 개념         | 연결 내용                                     |
| :------------ | :---------------------------------------- |
| **SRv6**      | VXLAN=데이터센터 내부 / SRv6=WAN 구간 / 경계에서 상호 연동 |
| **일관성 해싱**    | ECMP UDP 소스 포트 해시 = 일관성 해싱 원리 적용          |
| **CXL 3.0**   | 스파인-리프 언더레이 공유 / 메모리·네트워크 패브릭 통합          |
| **GARP**      | VM 이동 후 GARP로 VTEP MAC 테이블 갱신             |
| **NGFW·CASB** | VNI 기반 테넌트 트래픽 구분·보안 정책 적용                |

***

**(제언)** "VXLAN은 '물리 네트워크의 제약을 소프트웨어로 초월해 클라우드 규모의 멀티테넌트 네트워크를 구현하는 현대 데이터센터의 핵심 기반'입니다. **앞서 다룬 AIDC 특별법 기반 AI 데이터센터 구축 시 스파인-리프 언더레이 위에 VXLAN+BGP EVPN 오버레이를 구성해 AI 워크로드 테넌트 격리와 ECMP 기반 고대역폭 GPU 통신을 동시에 달성하고, Kubernetes CNI Cilium의 eBPF 기반 VXLAN으로 컨테이너 네트워크 성능을 최적화하며 앞서 다룬 CXL 3.0 메모리 패브릭과 VXLAN 네트워크 패브릭을 통합 설계하는 것이 차세대 AI 데이터센터 네트워크 아키텍처의 핵심 전략입니다.**"
