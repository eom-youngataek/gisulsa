### **IP 라우팅 핵심 알고리즘: 최장 프리픽스 매칭 (LPM)**

***

#### 답안 전체 스토리 흐름 (목차)

```
Ⅰ. 개요 (왜 CIDR 환경에서 LPM이 라우팅의 핵심인가)
Ⅱ. LPM 핵심 원리 및 동작
Ⅲ. 구현 자료구조 및 적용 체계
Ⅳ. 결론
```

포인트: 개요에서 \*\*"앞서 다룬 일관성 해싱이 '분산 시스템에서 키를 노드에 매핑'하는 알고리즘이라면, LPM(Longest Prefix Matching·최장 프리픽스 매칭)은 'IP 라우팅 테이블에서 목적지 IP와 가장 길게 일치하는 프리픽스(서브넷)를 선택하는 라우팅 결정 알고리즘'이다 — CIDR(Classless Inter-Domain Routing) 도입으로 가변 길이 서브넷 마스크(VLSM)가 가능해지면서 동일 목적지 IP에 여러 프리픽스가 매칭될 수 있고, 이 중 가장 구체적인(긴) 프리픽스를 선택하는 LPM이 라우터·스위치·방화벽·앞서 다룬 NGFW의 패킷 포워딩 핵심 메커니즘이며, 앞서 다룬 SRv6 세그먼트 라우팅·SDN 플로우 테이블 매칭의 이론적 기반"\*\*이라는 한 줄로 시작하면 전체 맥락이 드러납니다.

***

#### Ⅱ. LPM 핵심 원리 및 동작

**가. CIDR과 LPM 필요성**

```
[클래스풀 → CIDR 전환과 LPM 필요성]

클래스풀 (과거):
  Class A: /8  (16M 호스트)
  Class B: /16 (65K 호스트)
  Class C: /24 (254 호스트)
  → 고정 길이 → 단순 매칭·LPM 불필요

CIDR (현재):
  10.0.0.0/8
  10.1.0.0/16
  10.1.2.0/24
  10.1.2.128/25
  → 가변 길이 → 동일 IP에 복수 매칭 발생
  → LPM으로 최적 경로 선택 필요
```

***

**나. LPM 동작 예시**

```
[LPM 라우팅 테이블 예시]

라우팅 테이블:
  ①0.0.0.0/0      → 게이트웨이 (기본 경로)
  ②10.0.0.0/8     → 인터페이스 A
  ③10.1.0.0/16    → 인터페이스 B
  ④10.1.2.0/24    → 인터페이스 C
  ⑤10.1.2.128/25  → 인터페이스 D

목적지 IP: 10.1.2.200

매칭 결과:
  ①0.0.0.0/0:      매칭 ✅ (프리픽스 길이 0)
  ②10.0.0.0/8:     매칭 ✅ (프리픽스 길이 8)
  ③10.1.0.0/16:    매칭 ✅ (프리픽스 길이 16)
  ④10.1.2.0/24:    매칭 ✅ (프리픽스 길이 24)
  ⑤10.1.2.128/25:  매칭 ✅ (프리픽스 길이 25)
                    ↑ 가장 긴 프리픽스 → 선택
LPM 결과: ⑤ → 인터페이스 D 포워딩 ✅
```

***

**나. LPM 핵심 체계**

| **핵심 척도**                | **📊 LPM 원칙·수식 🚨**                                                                                             | **🔑 비트 마스크 연산 🚨**                                                                                                              | **🏁 동점 처리·우선순위 💯**                                                                                                 |
| :----------------------- | :-------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------- |
| **핵심 원칙**                | **가장 구체적 경로 우선**: 프리픽스 길이 ↑ = 더 구체적 경로 / /25 > /24 > /16 > /8 > /0 / **정확성 보장**: 더 긴 프리픽스 = 더 정확한 목적지 지정        | **매칭 연산**: (목적지IP) AND (서브넷마스크) = 네트워크 주소 / 결과 = 프리픽스 네트워크 주소면 매칭 / **예시**: 10.1.2.200 AND 255.255.255.128 = 10.1.2.128 → /25 매칭 | **동일 길이 복수 매칭**: AD(Administrative Distance) / 메트릭(홉수·대역폭) / ECMP(Equal Cost Multi-Path) 로드밸런싱 / 앞서 다룬 **일관성 해싱** 연계 |
| **기본 경로(Default Route)** | **0.0.0.0/0**: 모든 IP와 매칭 / 프리픽스 길이 0 → 항상 최저 우선순위 / 라우팅 테이블 미매칭 시 최후 수단 / **null 라우트**: 0.0.0.0/0 → null0 (블랙홀) | **호스트 라우트**: /32 = 단일 IP 완전 매칭 / LPM 최고 우선순위 / 앞서 다룬 **SDP 접근 제어** 호스트별 정책 / **요약 라우트**: 상위 프리픽스로 묶어 테이블 최적화                     | 앞서 다룬 **SRv6 SR-TE**: 세그먼트 리스트 = LPM 결과 체인 / **MPLS**: LPM으로 레이블 결정 / **SDN**: 플로우 테이블 매칭 = LPM 확장                   |

***

#### Ⅲ. 구현 자료구조 및 적용 체계

**가. LPM 구현 자료구조 비교**

| 자료구조                      | 원리              | 탐색 복잡도      | 특징               |
| :------------------------ | :-------------- | :---------- | :--------------- |
| **선형 탐색**                 | 테이블 순차 검색·최장 매칭 | O(N) 🚨     | 단순·소규모 테이블       |
| **이진 트라이(Trie)**          | 비트별 이진 트리 탐색    | O(W) W=주소길이 | IPv4=32·IPv6=128 |
| **압축 트라이(Patricia Trie)** | 단일 자식 노드 압축     | O(W)        | 메모리 효율↑ 실무 표준    |
| **TCAM**                  | 하드웨어 병렬 매칭      | **O(1)** ✅  | 라우터·스위치 전용 칩     |
| **해시 테이블**                | 정확한 매칭만 가능      | O(1)        | LPM 부적합 🚨       |

***

**나. Patricia Trie 동작 도식화**

```
[Patricia Trie (압축 이진 트라이) 예시]

라우팅 테이블:
  10.0.0.0/8
  10.1.0.0/16
  10.1.2.0/24

이진 표현:
  10.0.0.0/8:  00001010.00000000.00000000.00000000
  10.1.0.0/16: 00001010.00000001.00000000.00000000
  10.1.2.0/24: 00001010.00000001.00000010.00000000

Patricia Trie 구조:
        [비트0~7: 00001010] ← 공통 /8
              │
        [비트8~15 비교]
        0 /         \ 1
    10.0.0.0/8    [비트8~15: 00000001] ← /16
                        │
                  [비트16~23 비교]
                  0 /         \ 1
              10.1.0.0/16  10.1.2.0/24
                              │
                          (비트16~23: 00000010)

탐색: 10.1.2.200
  → 비트 0~7: 00001010 ✅ → 하위 노드
  → 비트 8~15: 00000001 ✅ → 하위 노드
  → 비트 16~23: 00000010 ✅ → 10.1.2.0/24 매칭
```

***

**다. 적용 체계 비교**

| 적용 영역          | LPM 활용 방식                    | 핵심 연계            |
| :------------- | :--------------------------- | :--------------- |
| **인터넷 코어 라우터** | BGP 테이블 90만↑ 프리픽스 TCAM 병렬 탐색 | TCAM O(1) 하드웨어   |
| **앞서 다룬 NGFW** | ACL·보안 정책 IP 범위 매칭           | LPM + 포트·프로토콜 복합 |
| **앞서 다룬 SRv6** | SID 128비트 LPM으로 세그먼트 결정      | IPv6 주소 공간 LPM   |
| **앞서 다룬 SDN**  | OpenFlow 플로우 테이블 와일드카드 매칭    | LPM 확장·다중 필드     |
| **클라우드 VPC**   | 서브넷 라우팅 테이블 LPM              | AWS/Azure 라우팅    |
| **앞서 다룬 CASB** | IP 기반 Shadow IT 정책 매칭        | LPM + 도메인 매칭     |

***

**라. CIDR 집약(Aggregation)과 LPM 관계**

| 항목             | 내용                                                            |
| :------------- | :------------------------------------------------------------ |
| **CIDR 집약 목적** | 라우팅 테이블 크기 축소 / 인터넷 라우팅 확장성                                   |
| **집약 예시**      | 10.1.0.0/24·10.1.1.0/24·10.1.2.0/24·10.1.3.0/24 → 10.1.0.0/22 |
| **LPM과 관계**    | 집약 경로(/22)와 구체적 경로(/24) 동시 존재 시 LPM이 /24 선택                   |
| **블랙홀 방지**     | 집약 후 구체적 경로 삭제 시 LPM이 상위 경로로 포워딩                              |
| **국내 연계**      | 앞서 다룬 **AIDC 특별법** 데이터센터 IP 블록 집약 관리                          |

***

**(제언)** "LPM은 '라우팅 테이블의 모호성을 가장 구체적인 경로 선택으로 해소하는 IP 포워딩의 핵심 알고리즘'입니다. **앞서 다룬 SRv6의 SID 128비트 LPM·NGFW ACL 매칭·SDN 플로우 테이블이 모두 LPM 원리를 기반으로 하며, 고성능 라우터에서 TCAM O(1) 병렬 매칭으로 초당 수백만 패킷을 처리하고 소프트웨어 라우터에서 Patricia Trie로 메모리 효율적 O(W) 탐색을 구현하는 것이 현대 네트워크 장비 설계의 핵심이며, CIDR 집약으로 라우팅 테이블을 최적화하면서 LPM으로 구체적 경로를 보장하는 균형 설계가 인터넷 라우팅 확장성의 핵심 전략입니다.**"
