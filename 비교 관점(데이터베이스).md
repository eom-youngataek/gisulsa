### **관점 1️⃣ 데이터 모델링·무결성 제약 및 정합성 설계 메커니즘 비교 (How it Models & Enforces)**

| **구성요소**       | **핵심 내용**                                  | **세부 키워드**                                                                                                                                                                                                                                     |
| -------------- | ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **데이터 구조화**    | 데이터를 논리적으로 어떻게 구조화하고 개념화하는가                | - **ERD (Entity Relationship Diagram)**: 업무 분석을 바탕으로 개체, 속성, 관계를 표준 표기법으로 도식화한 설계도  <br>- **식별/비식별 관계**: 부모 테이블의 PK가 자식 테이블의 PK로 상속되는가(식별) vs 일반 속성(FK)으로 참조되는가(비식별)  <br>- **CRUD 매트릭스**: 프로세스와 엔티티 간의 매핑을 통해 누락되거나 왜곡된 데이터 흐름이 없는지 정합성 교차 검증 |
| **데이터 정규화**    | 데이터 중복으로 인한 이상 현상을 어떻게 방지하는가               | - **1NF~3NF**: 원자값 확보(1NF), 주식별자에 완전 함수 종속(2NF), 주식별자 외 속성 간 이행 함수 종속 제거(3NF)  <br>- **BCNF & 4NF~5NF**: 결정자이면서 후보키가 아닌 것 제거(BCNF), 다치 종속 제거(4NF), 조인 종속 제거(5NF)  <br>- **이상현상 (Anomaly)**: 정규화를 거치지 않아 발생하는 삽입 이상, 삭제 이상, 갱신 이상 현상 방지         |
| **무결성 강제**     | 데이터의 정확성과 신뢰성을 시스템 레벨에서 어떻게 강제하는가          | - **개체 무결성 (Primary Key)**: 테이블 내 모든 행은 반드시 고유한 식별자를 가지며, Null 값을 가질 수 없음  <br>- **참조 무결성 (Foreign Key)**: 외래키 값은 참조하는 테이블의 기본키와 일치하거나, 혹은 전적으로 Null이어야 함  <br>- **도메인 무결성**: 특정 속성에 입력되는 값은 정의된 데이터 타입, 길이, 룰(Constraint), 범위 제약조건을 만족해야 함    |
| **비즈니스 룰 자동화** | 데이터 변경 시 특정 업무 로직을 데이터베이스 단에서 어떻게 자동 처리하는가 | - **트리거 (Trigger)**: 특정 테이블에 INSERT/UPDATE/DELETE 이벤트가 수행될 때 암묵적으로 자동 실행되는 저장 프로시저  <br>- **참조 무결성 전파 (Cascading)**: 부모 행 삭제/수정 시 자식 데이터를 연쇄 삭제(`ON DELETE CASCADE`) 또는 Null화                                                                  |

---

### **관점 2️⃣ 트랜잭션 병행제어·장애회복 및 처리 성능 평가 비교 (How well it Processes)**

| **구성요소**        | **핵심 내용**                            | **세부 키워드**                                                                                                                                                                                                                                                                   |
| --------------- | ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **트랜잭션 격리 제어**  | 동시성 제어 시 트랜잭션의 신뢰성과 성능을 어떻게 관리하는가    | - **ACID 원칙**: 원자성(Atomicity), 일관성(Consistency), 격리성(Isolation), 영속성(Durability) 보장  <br>- **격리수준 4단계**: Read Uncommitted, Read Committed, Repeatable Read, Serializable 설정 제어  <br>- **동시성 부작용**: Dirty Read(커밋 전 조회), Non-Repeatable Read(중도 변경), Phantom Read(행 추가) 탐지 차단 |
| **병행 제어 및 교착**  | 여러 사용자가 동시에 접근할 때 데이터 일관성을 어떻게 유지하는가 | - **2단계 락킹 (2PL)**: 락을 획득만 하는 확장 단계와 락을 해제만 하는 수축 단계로 구분하여 직렬 가능성 보장  <br>- **MVCC**: 언두(Undo) 영역에 변경 전 데이터 버전(스냅샷)을 유지해 읽기 작업이 쓰기 작업을 방해하지 않게 동시성 극대화  <br>- **데드락 (Deadlock)**: 순환 대기 상태를 실시간 탐지하고 대기 그래프를 분석해 트랜잭션 롤백 처리(Wait-Die, Wound-Wait)                            |
| **장애 회복 및 저널링** | 시스템 다운 시 유실 없는 데이터 복구를 어떻게 보장하는가     | - **WAL (Write-Ahead Logging)**: 더티 페이지 디스크 기록 전, 변경 정보를 담은 로그 버퍼를 디스크 로그 파일에 우선 기록  <br>- **REDO / UNDO**: 시스템 정전 복구 시 커밋된 데이터는 다시 쓰고(REDO), 커밋되지 않은 미완성 데이터는 롤백(UNDO)  <br>- **체크포인트 (Checkpoint)**: 주기적으로 버퍼의 변경 사항을 디스크에 물리적으로 플러시하여 복구 대상 로그 범위 축소                      |
| **처리 성능 지표**    | 데이터베이스 전송 및 연산 속도를 어떻게 최적화하는가        | - **TPS (Transactions Per Second)**: 시스템이 초당 처리 완료 가능한 읽기/쓰기 복합 트랜잭션의 총 건수  <br>- **복구 시간 (RTO)**: 장애 발생 시점부터 WAL 및 REDO/UNDO 기법을 통해 서비스 정합성을 복구 완료하기까지 걸리는 시간  <br>- **더티 페이지 비율**: 버퍼 풀 내부에서 디스크와 동기화되지 않고 메모리상에서 갱신된 블록의 비율 관리                                            |

---

### **관점 3️⃣ 인덱스 아키텍처·구조 및 튜닝 기술별 최적 적용 조건 비교 (When to Use)**

| **구성요소**        | **핵심 내용**                        | **세부 키워드**                                                                                                                                                                                                                                                    |
| --------------- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **인덱스 아키텍처**    | 어떤 데이터 분포와 쿼리 유형에 각 인덱스가 가장 적합한가 | - **B+Tree 인덱스**: 모든 리프 노드가 연결 리스트로 매핑되어 등가 검색(==) 및 인덱스 범위 스캔(Range Scan)에 탁월  <br>- **클러스터드 인덱스**: 테이블당 단 1개만 생성 가능하며, 물리 데이터 정렬 순서와 인덱스 키 순서가 일치(범위 검색 유리)  <br>- **비트맵 인덱스**: 성별, 부서 등 데이터 분포도(Cardinality)가 극도로 낮은 컬럼의 대용량 의사결정계(DW/OLAP) 조회 최적          |
| **정규화 vs 반정규화** | 설계 구조 최적화 시 정규화와 반정규화의 트레이드오프    | - **정규화**: 데이터 중복 제거로 저장 용량을 최적화하고 정합성을 향상시키나, 조회 시 조인(Join) 연산량 폭증 문제 발생  <br>- **반정규화**: 쿼리 조인 병목을 해결하기 위해 의도적으로 중복 컬럼 추가, 테이블 분할/통합 수행(데이터 갱신 비용 증가)                                                                                                       |
| **최적화 엔진 및 경로** | 데이터베이스가 쿼리를 어떤 경로로 탐색하는가         | - **비용기반 옵티마이저 (CBO)**: 테이블 통계(카디널리티, 블록 수)를 바탕으로 실행 예상 비용(Cost)을 계산해 최적 계획 수립  <br>- **규칙기반 옵티마이저 (RBO)**: 사전에 설정된 15가지 우선순위 규칙에 따라 쿼리 조회 실행 경로를 정적으로 결정  <br>- **실행 계획 (Execution Plan)**: 조인 방식(Nested Loop, Hash Join) 및 인덱스 활용 여부를 튜닝 단계에서 프로파일링 검증      |
| **구조적 한계·위험**   | 데이터베이스 설계 구조가 가지는 고유한 리스크는 무엇인가  | - **인덱스 분할 (Page Split)**: B+Tree 리프 페이지가 채워진 상태에서 추가 데이터 삽입 시 페이지가 쪼개져 디스크 쓰기 부하 유발  <br>- **테이블 풀 스캔 (Full Scan)**: 인덱스가 누락되었거나 통계 정보 오류로 옵티마이저가 전체 디스크 블록을 모두 읽어 성능 급락  <br>- **락 블로킹 (Blocking)**: 특정 트랜잭션이 테이블/로우에 베타 락(X-Lock)을 유지하여 다른 사용자가 무한 대기하는 현상 |

---

### **관점 4️⃣ 물리 모델 튜닝·최적화 및 데이터베이스 운영 비교 (How to Optimize)**

| **구성요소**       | **핵심 내용**                      | **세부 키워드**                                                                                                                                                                      |
| -------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **테이블 파티셔닝**   | 대용량 테이블을 물리 레벨에서 어떻게 세분화하는가    | - **테이블 파티셔닝**: 단일 대형 테이블을 물리적으로 범위(Range), 목록(List), 해시(Hash) 기준으로 쪼개어 개별 파일 보관  <br>- **파티션 프루닝 (Pruning)**: 쿼리 조건절 필터에 해당하는 특정 파티션 블록만 한정해서 스캔하여 디스크 I/O를 획기적으로 차단           |
| **메모리 영역 최적화** | 디스크 I/O를 줄이기 위해 메모리를 어떻게 관리하는가 | - **공유 버퍼 풀 (Buffer Pool)**: 디스크 블록을 메모리 영역에 캐싱하고 LRU 알고리즘 기반으로 관리하여 디스크 읽기 최소화  <br>- **로그 버퍼 (Log Buffer)**: 데이터베이스 커밋 시 디스크에 순차적 동기 쓰기를 수행하기 전 로그 변경 기록을 임시 캐싱               |
| **세그먼트 정돈**    | 단편화로 인한 디스크 낭비를 어떻게 개선하는가      | - **HWM (High Water Mark) 정리**: 테이블 데이터 삭제 후 공간이 비어도 유지되는 최고 수위를 Shrink/Reorg 명령으로 회수  <br>- **인덱스 리빌드 (Rebuild)**: 삽입/삭제 반복으로 인덱스 리프 페이지 간의 조각남(Fragmentation) 현상이 심할 때 재구성 튜닝 |
| **통계 정보 갱신**   | 옵티마이저의 올바른 판단을 위해 어떤 튜닝을 수행하는가 | - **DBMS 통계 수집**: 정기적으로 데이터의 분포 상태를 수집하여 옵티마이저가 잘못된 인덱스를 타는 현상(Optimizer 오류) 방지  <br>- **슬로우 쿼리 로그**: 시스템 리소스를 과점하는 장시간 수행 쿼리(Slow Query)를 프로파일링하여 인덱스/조인 구조 튜닝 타깃팅             |

---

### **관점 5️⃣ 데이터 보안·규제 준수·운영 신뢰성 비교 (How to Secure)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**데이터 암호화**|데이터베이스 내 중요 정보를 어떻게 암호화하여 보호하는가|- **TDE (Transparent Data Encryption)**: 데이터베이스 저장 파일(Data Files) 및 테이블스페이스 자체를 암호화(OS 백업본 탈취 예방)  <br>- **컬럼 암호화**: 주민번호, 패스워드 등 민감 컬럼에 API 방식 또는 플러그인 방식으로 단방향(SHA)/양방향(AES) 암호화 적용|
|**접근 제어 및 감사**|데이터베이스에 대한 비인가 접근을 어떻게 제어하고 감시하는가|- **RBAC (역할 기반 접근제어)**: 직무별 권한 그룹(Role)을 생성하고 각 스키마의 DDL, DML 실행 권한을 선별 부여(`GRANT`)  <br>- **데이터베이스 감사 (Audit)**: 중요 테이블에 쿼리를 실행한 사용자의 세션 정보, 쿼리문, 접근 시간, 성공 여부를 감사 로그 저장  <br>- **SQL Injection 차단**: 소스코드 단에서 바인드 변수(Bind Variable)를 강제해 사용자 입력을 쿼리 명령어로 오인하는 사고 예방|
|**데이터 유출 차단**|데이터 무단 반출을 방지하기 위해 어떤 규정을 충족하는가|- **개인정보 고유식별정보 암호화**: 개인정보보호법에 의거 주민등록번호, 여권번호 등은 반드시 디스크 저장 시 암호화 조치 강제  <br>- **데이터 마스킹 (Masking)**: 응용 애플리케이션 화면 표시 시 주민번호 뒷자리나 카드번호 중간을 아스테리스크(`*`) 처리|
|**이중화 및 백업**|재해 발생 시 데이터 유실 방지를 위해 어떻게 구성하는가|- **복제 (Replication)**: 실시간 트랜잭션 로그를 대기 서버로 전송해 동기화(Active-Active, Active-Standby 이중화망 구성)  <br>- **PITR (Point-in-Time Recovery)**: 전체 백업본과 아카이브 로그 파일을 조합하여 특정 시간, 특정 시점으로 완벽하게 롤백 복구|

---

### **📋 관계형 데이터베이스 핵심 메커니즘 전체 비교 요약표**

|**비교 항목**|**정규화 (1NF~5NF)**|**인덱스 (B+Tree)**|**트랜잭션 제어 (2PL/MVCC)**|**장애 회복 (WAL/Checkpoint)**|
|---|---|---|---|---|
|**관련 영역**|데이터베이스 논리 모델 설계|물리적 조회 성능 최적화|동시성 및 데이터 정합성 보장|데이터베이스 안정성 및 장애 복구|
|**핵심 역할**|중복 최소화 및 데이터 이상 방지|특정 값 및 범위 스캔 속도 향상|다중 접근 시 데이터 무결성 보장|시스템 정전 시 데이터 유실 방지|
|**동작 메커니즘**|함수적 종속성에 따른 릴레이션 분할|루트-브랜치-리프 노드 탐색 스캔|Lock을 획득/수축하거나 버전 관리|로그 선기록(WAL) 후 REDO/UNDO 복구|
|**성능 영향도**|입력/수정 성능 향상, 조인 증가로 조회 감소|인덱스 스캔을 통한 디스크 I/O 절감|락 대기 지연(Blocking), 데드락 리스크|로그 기록 성능(Disk I/O)이 쓰기 장벽 유발|
|**보안/제약 연계**|도메인, 개체, 참조 무결성 준수|인덱스 컬럼 암호화 시 인덱스 스캔 불가|Serializable 격리 시 강력한 보안 유지|WAL 파일 암호화를 통한 파일 탈취 방지|
|**핵심 한계**|중복 제거로 인해 쿼리 조인 연산 폭증|데이터 변경(CUD) 시 인덱스 갱신 부하|MVCC의 경우 언두 영역 비대화 오버헤드|체크포인트 실행 시 일시적 시스템 지연|
|**대표 적용 시나리오**|신규 비즈니스 테이블 스키마 설계|대규모 사용자 로그 테이블 검색 속도 튜닝|금융권 계좌 이체, 예매 동시성 제어|정전 대비 가용성 서버 복구 아키텍처|
|**주요 평가 지표**|중복 데이터 비율, 조인 뎁스(Depth)|인덱스 단편화율, 스캔 블록 수|동시 트랜잭션 수(TPS), 대기 시간(Wait)|복구 목표 시간(RTO), 체크포인트 주기|


### **관점 1️⃣ SQL 명령어·실행 아키텍처 비교 (How it Structures)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**SQL 명령어 분류**|데이터베이스를 제어하는 명령어 체계가 어떠한가|- **DDL (정의어)**: 데이터베이스 객체를 생성/수정/삭제하여 물리적 스키마 구조를 설계(CREATE, ALTER, DROP)  <br>- **DML (조작어)**: 테이블 내 레코드를 실시간 조작하고 조회하는 핵심 명령어(SELECT, INSERT, UPDATE, DELETE)  <br>- **DCL (제어어)**: 데이터베이스 자원에 대한 접근 및 조작 권한을 부여하고 회수(GRANT, REVOKE)  <br>- **TCL (트랜잭션어)**: 데이터 상태 변경 작업을 커밋하거나 롤백하여 트랜잭션의 완결성을 최종 승인(COMMIT, ROLLBACK)|
|**세션 파싱 아키텍처**|SQL 쿼리가 입력되어 실행되기까지의 과정을 어떻게 조율하는가|- **소프트 파싱**: 공유 풀(Shared Pool)의 라이브러리 캐시에서 기컴파일된 실행 계획을 찾아 재사용하여 파싱 시간 제거  <br>- **하드 파싱**: 실행 계획이 없어 구문 분석, 권한 검사, 통계 정보 계산을 처음부터 새로 만드는 CPU 집약적 연산  <br>- **공유 풀**: 실행 계획을 캐싱하는 라이브러리 캐시와 시스템 메타데이터를 저장하는 딕셔너리 캐시로 구성된 메모리 영역|
|**조인 연결 메커니즘**|어떤 방식으로 여러 테이블의 데이터 릴레이션을 물리적으로 결합하는가|- **Nested Loop Join**: 선행 테이블(Driving) 1행마다 후행 테이블(Driven)의 인덱스를 반복 탐색하는 중첩 루프 방식  <br>- **Hash Join**: 선행 테이블을 기반으로 메모리에 해시 맵(Build)을 구성하고 후행 테이블을 읽어 해시 키 비교(Probe)  <br>- **Sort Merge Join**: 양 테이블을 조인 컬럼 기준으로 각각 선행 정렬한 후, 스캐닝하며 차례대로 병합하여 결합|
|**서브쿼리 실행 구조**|서브쿼리가 메인 쿼리와 어떻게 결합되어 실행되는가|- **상관 서브쿼리**: 메인 쿼리의 데이터 행을 참조하여 서브쿼리가 매 행마다 반복 실행되는 연동 구조  <br>- **서브쿼리 언네스팅**: 옵티마이저가 서브쿼리를 일반 조인 문장으로 풀어 변환하여 최적의 실행 계획 경로 탐색 유도  <br>- **인라인 뷰**: FROM 절의 서브쿼리로 메모리 내 임시 가상 테이블처럼 동작하며 서브쿼리 결과 집합을 먼저 한정|

---

### **관점 2️⃣ 쿼리 성능·리소스 제어 및 품질 평가 비교 (How well it Executes)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**속도 및 지연 성능**|쿼리 전송 및 조회 응답 지연을 어떻게 단축하는가|- **응답 시간 (Response Time)**: 쿼리 실행 후 첫 번째 행이 반환되어 화면에 출력될 때까지 소요되는 지연  <br>- **논리적 I/O (LIO)**: 디스크 접근 전 메모리 버퍼 풀(Buffer Pool) 영역에서 데이터를 조회하여 읽은 총 블록 수  <br>- **물리적 I/O (PIO)**: 버퍼 풀에 데이터가 없어 실제 디스크 저장 장치에서 메모리로 적재하며 읽은 블록 수|
|**정확성 및 처리 평가**|정량적 쿼리 수행 능력과 정합성을 어떻게 평가하는가|- **처리량 (Throughput)**: 단위 시간당 완결한 총 SQL 쿼리의 건수(TPS 등)  <br>- **디스크 덤프**: 정렬/해시 연산 데이터가 PGA 메모리 크기를 초과해 Temp Tablespace 가상 디스크 영역을 쓸 때의 오버헤드  <br>- **실행 계획 정합성**: 옵티마이저가 설계 의도대로 인덱스를 스캔하고 풀 테이블 스캔을 방지하고 있는지 여부 검증|
|**고급 함수 분석 성능**|고난도 연산 시 어떤 함수를 선택해 쿼리를 최적화하는가|- **Window Function**: 기존 GROUP BY와 달리 행의 원본 수를 보존하며 순위(RANK), 누계(SUM OVER) 등의 복잡한 계산 수행  <br>- **CTE (With 절)**: 임시 뷰를 `WITH` 절로 선언해 가독성을 대폭 향상시키고 계층 구조 조회를 위한 재귀(Recursive) 쿼리 지원  <br>- **스칼라 서브쿼리**: SELECT 절에 위치하여 서브쿼리 입력값에 대한 반환값을 메모리에 캐싱(Scalar Subquery 캐싱 효과)|
|**동시성 및 대기 평가**|다중 사용자의 동시 쿼리 수행 품질을 어떻게 감시하는가|- **대기 이벤트 (Wait Event)**: 쿼리 경합 시 발생하는 래치 획득 지연, 버퍼 경합, 락(Lock) 대기 등의 프로파일링 로그 수집  <br>- **세션 병목**: 특정 쿼리가 리소스를 과점하여 대기 세션 수 및 락 대기 시간(Lock Wait Time)이 임계치를 초과하는 현상|

---

### **관점 3️⃣ 조인 및 고급 쿼리별 최적 환경·적용 조건 비교 (When to Use)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**조인별 최적 태스크**|어떤 조회 시나리오에 각 쿼리 기법이 가장 적합한가|- **Nested Loop**: 드라이빙 테이블의 크기가 작고 후행 테이블에 완벽한 **인덱스**가 구비된 OLTP성 실시간 단건 조회  <br>- **Hash Join**: 대용량 테이블 간의 결합이며 조인 조건이 동등(==) 조건이고, 인덱스가 없는 DW/OLAP 배치 분석  <br>- **Sort Merge**: 조인 조건이 범위(≥≥, ≤≤) 조건이거나, 이미 양쪽 테이블이 조인 키 기준으로 정렬되어 있는 경우  <br>- **구체화 뷰 (MView)**: 일일 마감 통계 등 자주 변경되지 않지만 복잡한 집계가 필요한 대용량 배치 요약 데이터 조회|
|**데이터 가상화 3대 비교**|일반 View vs Updatable View vs Materialized View|- **물리 저장**: MView(물리 디스크 저장 공간 점유 ✅) >> 일반 View / Updatable View(논리적 정의만 공유)  <br>- **DML 제약**: 일반 View(기저 연동 가능) >> Updatable View(제한적 DML 허용) >> MView(DML 직접 수행 불가, 동기화만 허용)  <br>- **조회 속도**: MView(미리 집계되어 최고 속도) >> 일반 View(매번 기저 테이블 변환 및 쿼리 파싱 수행)|
|**SQL 실행 방식 선택**|정적 SQL과 동적 SQL의 활용 매핑 차이|- **정적 SQL**: 동일한 쿼리 골격에 조건 값만 바뀌어 빈번히 실행되는 OLTP성 트랜잭션 애플리케이션 서비스(TFO)  <br>- **동적 SQL**: 다차원 조건 검색(선택 조건에 따라 WHERE 절 구조가 가변적으로 변함)을 제공해야 하는 포털 상세 검색 쿼리|
|**구조적 한계·위험**|각 쿼리 기법 적용 시 발생하는 부작용과 성능 리스크는 무엇인가|- **인덱스 컬럼 변형**: WHERE 절 조건절의 인덱스 컬럼 가공(예: `SUBSTR(phone, 1, 3)`)으로 인덱스가 안 타고 Full Scan 발생  <br>- **힌트(Hint) 오류**: 강제 지정한 힌트(예: `/*+ USE_NL */`)가 통계 정보 변경으로 최악의 계획을 실행해도 무조건 기동되는 리스크  <br>- **동기화 오버헤드**: MView 갱신 주기를 `ON COMMIT`으로 과도하게 촘촘히 설정 시 기저 테이블 트랜잭션 처리 지연 유발|

---

### **관점 4️⃣ 쿼리 튜닝·옵티마이저 최적화·운영 제어 비교 (How to Optimize)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**파싱 최적화**|하드 파싱 지연을 어떻게 해소하고 최적화하는가|- **바인드 변수 강제화**: 변수값만 변경하며 쿼리 뼈대를 재사용하게 하여 공유 풀 내 라이브러리 캐시 힛(Hit)율 극대화  <br>- **리터럴 쿼리 차단**: 매번 상수값을 조립하는 인라인 쿼리를 차단하여 하드 파싱 지연 및 메모리 고갈 방지|
|**옵티마이저 실행 제어**|옵티마이저가 최적의 탐색 경로를 타도록 어떻게 통제하는가|- **비용기반 옵티마이저 (CBO)**: 주기적 통계 정보(`DBMS_STATS`) 수집을 토대로 디스크 I/O 및 CPU 예상 비용으로 경로 수립  <br>- **규칙기반 옵티마이저 (RBO)**: 사전 정의된 15단계 우선순위 규칙에 따라 쿼리 실행 계획 경로를 정적으로 지정  <br>- **SQL 힌트 (Hints)**: `/*+ USE_HASH(A B) */` 등을 쿼리에 작성하여 개발자가 직접 조인 방식 및 스캔 경로 강제 지정|
|**실행 계획 프로파일링**|실제 수행되는 쿼리 계획을 어떻게 최적화하는가|- **EXPLAIN PLAN**: SQL 실행 이전에 예상 액세스 경로(Index Scan vs Full Table Scan) 검증 및 인덱스 누락 확인  <br>- **DBMS_XPLAN**: 실제 런타임 단계에서 읽어낸 논리적/물리적 블록 수와 단계별 처리 경로 정합성 프로파일링  <br>- **조인 방식 교정**: NL 조인의 드라이빙 테이블을 변경하거나, 대용량 정렬 연산이 의심될 시 Hash 조인으로 튜닝 변경|
|**MView 동기화 최적화**|Materialized View의 동기화 오버헤드를 어떻게 튜닝하는가|- **Fast Refresh**: 변경분 데이터 로그(MView Log)만 정밀 추적하여 뷰 변경 사항만 부분적으로 빠르게 업데이트 튜닝  <br>- **Complete Refresh**: 전체 구체화 뷰를 처음부터 다시 재작성하는 튜닝 방식(대용량 정합성 완전 확보 시 사용)  <br>- **ON DEMAND**: 트랙픽 유입이 적은 새벽 시간 등 배치 주기별로 사용자가 수동/자동 동기화 명령을 내리도록 통제|

---

### **관점 5️⃣ 쿼리 보안·무결성 통제·규제 준수 비교 (How to Secure)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**SQL 주입 공격 차단**|쿼리 구조 왜곡을 통한 공격을 어떻게 탐지 방어하는가|- **Prepared Statement**: 사용자 입력값을 단순 데이터 리터럴 상수로 바인딩 처리하여 쿼리 자체 구조 왜곡 차단  <br>- **동적 SQL 검증**: 문자열 연산(`+`)을 통해 즉석으로 기동되는 동적 SQL 문장을 엄격히 검사하고 특수문자 차단 필터 적용  <br>- **화이트리스트 입력 필터**: 입력 폼에 싱글 쿼테이션(`'`), 세미콜론(`;`), 주석(`--`) 등의 메타문자 유입을 차단하는 정규식 적용|
|**데이터 가상화 보안**|민감 데이터를 인프라 수준에서 어떻게 차단하는가|- **보안 뷰 (Secure View)**: 개인정보, 민감 재무 정보 등 특정 컬럼을 제외하고 뷰를 정의하여 사용자의 원본 테이블 접근 차단  <br>- **DCL 권한 통제**: `GRANT SELECT ON secure_view TO role_user`를 통해 업무 목적별 최소 권한 통제 모델 준수|
|**DML 무결성 통제**|데이터 변경 시 신뢰성을 어떻게 보증하는가|- **참조 무결성 전파 (Cascading)**: `ON DELETE CASCADE` 등 설정을 통해 부모 행 삭제 시 고아 데이터를 남기지 않는 정합성 보장  <br>- **트리거 통제**: 데이터 중요 변경(INSERT, UPDATE 등) 감지 시 자동으로 변경 이력용 테이블에 이력을 적재하도록 보안 구성|
|**개인정보 규제 준수**|개인정보보호 규정을 쿼리 레벨에서 어떻게 충족하는가|- **마스킹 처리**: SELECT 조회 시 개인정보 뒷자리를 마스킹하여(예: `SUBSTR`) 애플리케이션 단으로 복호화 평문이 넘어가지 않게 제어  <br>- **암호화 함수 연동**: 쿼리 삽입/수정 시 DBMS 내부 대칭키 암호화 함수를 실행해 저장 파일 도난 시에도 유출되지 않게 조치|

---

### **📋 핵심 SQL 쿼리 및 조인 기법 전체 비교 요약표**

|**비교 항목**|**일반 뷰 (View)**|**구체화 뷰 (MView)**|**Nested Loop Join**|**Hash Join**|
|---|---|---|---|---|
|**관련 영역**|데이터 가상화 (논리 격리)|데이터 가상화 (조회 최적화)|데이터 조인 기법 (OLTP 환경)|데이터 조인 기법 (OLAP/대용량)|
|**물리적 저장 유무**|없음 (가상 쿼리만 보존)|있음 (실제 결과를 물리 디스크에 보존) ✅|없음 (메모리 런타임 결합)|없음 (PGA 메모리상 해시 결합)|
|**동작 메커니즘**|호출 시 기저 테이블 쿼리로 치환 실행|미리 계산된 물리적 테이블 조회|선행 테이블 한 행당 후행 인덱스 반복 스캔|선행 테이블로 해시 맵 생성 후 후행 매칭|
|**성능 영향도**|단순 조회 래퍼로 성능 오버헤드 미미|대용량 배치/요약 쿼리 조회 성능 대폭 향상 ✅|데이터가 소량이고 인덱스가 있을 때 가장 빠름 ✅|대용량 정렬 안 된 데이터 조인 시 성능 우수 ✅|
|**인프라/메모리 부하**|추가적인 리소스 부하 없음|저장 공간(Disk) 및 동기화 시 CPU 부하 유발|후행 테이블 랜덤 액세스로 디스크 I/O 유발|해시 테이블 생성을 위한 대량의 PGA 메모리 소모 🚨|
|**핵심 한계**|뷰 내부 조인/집계 시 DML(Updatable) 원천 제한|기저 데이터 변경 시 정합성 불일치 리스크|후행 테이블 인덱스 부재 시 Full Scan 폭락|해시 테이블이 PGA 크기 초과 시 성능 급락|
|**대표 적용 시나리오**|민감 데이터 컬럼 은닉 및 보안 필터 적용|일일 통계 매출 요약 대시보드 데이터 조회|고객 ID 기반의 단건 주문 내역 조회|대규모 로그 테이블과 사용자 정보 조인 분석|
|**주요 평가 지표**|뷰 계층 구조 뎁스(Depth), 파싱 시간|동기화 주기(Refresh), 디스크 점유 용량|랜덤 액세스 수, 블록 I/O(Logical Reads)|해시 맵 빌드 시간, Temp Tablespace 사용량|


### **관점 1️⃣ 분산 아키텍처·데이터 합의 및 일관성 메커니즘 비교 (How it Consents)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**분산 합의**|분산 노드 간의 일관된 상태 합의를 어떻게 이끌어내는가|- **Paxos**: 리더가 제안하고 과반수(Quorum) 수락으로 동의를 얻어 정합성을 보장하는 전통적 분산 합의 알고리즘  <br>- **Raft**: Paxos의 복잡성을 개선하여 Strong Leader 선출 및 로그 복제(Replication) 과정을 직관화한 알고리즘  <br>- **PBFT**: 악의적인 노드(배신자 노드)가 존재할 수 있는 비신뢰 네트워크에서 과반수 검증을 통해 합의 도출|
|**분산 아키텍처**|분산 데이터베이스가 어떤 물리적/논리적 결합 구조를 취하는가|- **Shared Nothing**: 노드 간에 메모리와 디스크를 전적으로 공유하지 않고 독립 가동하여 수평 확장에 극히 유리  <br>- **Master-Slave (Primary-Secondary)**: 마스터 노드는 쓰기 처리를 전담하고, 슬레이브 노드는 데이터 복제본으로 읽기만 전담  <br>- **Multi-Master**: 모든 분산 노드가 읽기/쓰기를 동시에 처리하여 특정 노드 장애 시에도 가용성 지속 보장|
|**복제 및 일관성**|데이터 변경 시 복제 일관성을 어떻게 관리하는가|- **강한 일관성 (Strong)**: 모든 노드에 쓰기가 완료될 때까지 조회를 대기시켜 데이터 완전 동기 보장(정합성 최고)  <br>- **최종 일관성 (Eventual)**: 일시적인 정합성 불일치를 허용하되, 최종적으로는 모든 노드에 동기화되는 모델(속도 최고)  <br>- **Quorum Read/Write**: 읽기 노드(RR)와 쓰기 노드(WW)의 합이 전체 노드 수(NN)보다 크도록 설정해 정합성 보장(R+W>NR+W>N)|
|**CAP 및 PACELC**|장애 상황 시 어떤 구조적 특성을 선택하는가|- **CAP 이론**: 일관성(C), 가용성(A), 분할 내성(P) 중 네트워크 장애 상황(P)에서 최대 2가지만 선택 가능함을 증명  <br>- **PACELC 이론**: 장애 시(P) A와 C의 트레이드오프를 따지고, 정상 상황(E)일 때는 L(Latency)과 C의 트레이드오프를 평가|

---

### **관점 2️⃣ 분산 트랜잭션·수평 확장 및 가용성 평가 비교 (How well it Scales)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**분산 트랜잭션**|여러 노드에 걸쳐 실행되는 트랜잭션의 원자성을 어떻게 보장하는가|- **2단계 커밋 (2PC)**: 조정자(Coordinator) 주도로 모든 참여 노드에 준비(Prepare)를 묻고 합의 시 일괄 커밋(Commit) 수행  <br>- **3단계 커밋 (3PC)**: 2PC의 조정자 다운 시 블로킹(Blocking) 한계를 개선하고자 예비 커밋(Pre-Commit) 단계를 추가  <br>- **Saga Pattern**: 마이크로서비스 환경에서 각 로컬 트랜잭션을 순차 실행하고 실패 시 보상 트랜잭션(Compensating)을 연쇄 호출|
|**수평 확장**|시스템 규모 확장 시 병목을 어떻게 극복하는가|- **Scale-Out 가용성**: 무중단 상태에서 신규 노드를 추가하여 동적으로 쓰기/읽기 대역폭을 확장하는 성능  <br>- **처리량 (Throughput)**: 분산 노드 확장에 따른 초당 처리 가능한 읽기/쓰기 건수의 선형적 비례 상승도(Linear Scalability)|
|**트래픽 분산 지연**|분산 처리 시 발생하는 지연 오버헤드는 어떠한가|- **네트워크 지연 (Network Latency)**: 합의 및 복제 과정에서 발생하는 노드 간 네트워크 왕복시간(RTT) 누적으로 인한 트랜잭션 지연  <br>- **데이터 핫스팟 (Hotspot) 지연**: 파티션 키(Partition Key) 설계 오류로 특정 분산 노드에만 트래픽이 집중되어 발생하는 지연|
|**자동 장애 복구**|노드 장애 시 시스템 다운 타임을 어떻게 최소화하는가|- **자동 장애 조치 (Failover)**: 마스터 노드 장애 발생 시 Raft/Paxos 합의 알고리즘에 의해 신규 마스터 리더를 선출하는 속도  <br>- **Split-Brain 방지**: 네트워크 단절 시 양측 분할 구역이 모두 마스터를 자처하는 오류를 과반수 룰(Quorum)로 원천 차단|

---

### **관점 3️⃣ 분산DB 모델별 최적 환경·적용 조건 비교 (When to Use)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**분산DB 최적 태스크**|어떤 데이터 비즈니스 아키텍처에 각 분산 기술이 가장 적합한가|- **NewSQL (Spanner)**: 글로벌 금융 시스템 등 강력한 일관성(ACID)과 분산 수평 확장이 동시에 필요한 코어 인프라  <br>- **Key-Value (Redis)**: 고속 세션 정보 보관, 캐시 레이어 구성, 단순 키값 위주의 초고속 임시 데이터 저장  <br>- **Document (MongoDB)**: 소셜 커머스 상품 정보 등 스키마 변화가 잦고 빠른 개발과 비정형 데이터 처리가 필요한 환경  <br>- **Wide-Column (Cassandra)**: IoT 센서 데이터 등 초고속 대용량 시계열 로그 적재 및 대규모 쓰기 특화 무중단 아키텍처|
|**CAP 기반 선택**|CP 시스템과 AP 시스템의 활용 매핑 차이|- **CP System**: 정합성이 절대적이어야 하는 결제, 금융 이체, 상품 수량 관리 등의 도메인(HBase, MongoDB, Redis)  <br>- **AP System**: 일시적 데이터 불일치를 허용하더라도 24시간 가용 서비스가 끊김 없이 돌아가야 하는 SNS, 쇼핑몰 장바구니(Cassandra)|
|**데이터 파티셔닝**|데이터를 물리 노드에 분산할 때 어떤 라우팅 규칙을 선택하는가|- **범위 기반 샤딩 (Range)**: 특정 키 범위를 기준으로 노드 분산(스캔 범위 제어 용이하나 특정 범위에 부하 집중 리스크)  <br>- **해시 기반 샤딩 (Hash)**: 해시 함수 적용 결과값으로 노드를 강제 분산(데이터 균등 분배 강점 있으나 범위 스캔 비효율)  <br>- **일관된 해싱 (Consistent Hashing)**: 해시 링 구조를 활용해 분산 노드 증설/삭제 시 데이터 재배치 비용을 최소화하는 기술|
|**구조적 한계·위험**|각 분산 데이터베이스 아키텍처가 노출하는 구조적 리스크는 무엇인가|- **2PC 블로킹**: 분산 2PC 수행 도중 조정자(Coordinator)가 불능 상태에 빠지면 모든 참여 노드가 락을 쥔 채 대기하는 리스크  <br>- **최종 일관성 오차**: Eventual Consistency 환경에서 계좌 잔액 차감 시 실시간 정합성 결여로 인한 중복 출금 등 금융 사고 유발  <br>- **쓰기 증폭 현상**: 복제 노드가 너무 많을 경우 하나의 쓰기 트랜잭션이 여러 노드에 동기화 쓰기를 발생시켜 전체 처리량 저하|

---

### **관점 4️⃣ 데이터 샤딩·분산 최적화 및 시스템 운영 비교 (How to Optimize)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**인메모리 분산 캐시**|데이터베이스 전면에서 분산 캐시를 어떻게 최적화하는가|- **정합성 정책**: Write-Through(메모리와 DB 동시 쓰기), Write-Back(메모리 먼저 기록 후 지연 쓰기), Write-Around 전략 튜닝  <br>- **캐시 스탬피드**: 대량 캐시 만료 시 기저 DB로 쿼리가 폭증하는 현상을 막기 위해 캐시 갱신 락(Lock) 및 확률적 조기 갱신 튜닝|
|**샤드 리밸런싱**|노드 추가 시 데이터 재분배를 어떻게 최적화하는가|- **가상 노드 (Vnodes)**: 실제 물리 노드 한 대당 수십~수백 개의 가상 토큰 범위를 매핑하여 데이터를 물리 장비에 균등 재분배  <br>- **무중단 마이그레이션**: 신규 샤드 노드 편입 시 기존의 읽기/쓰기를 정상 가동하며 가상 링에서 데이터를 백그라운드 동기화|
|**쿼리 라우팅 최적화**|분산 쿼리 요청 경로를 어떻게 최적화하는가|- **파티션 키 필터링**: 쿼리 조건절에 샤드 키(Shard Key)를 포함시켜 모든 노드에 쿼리를 던지는 스캐터-게더(Scatter-Gather) 방지  <br>- **라우팅 중개자**: MongoDB의 Mongos와 같이 클라이언트 요청을 가로채어 최적의 샤드 노드로 다이렉트 쿼리를 전송하는 허브|
|**분산 복제 지연 감시**|분산망 상태를 어떻게 관제하는가|- **복제 랙 (Replication Lag)**: 마스터 노드에 쓰인 데이터가 슬라이브 노드로 전송되어 반영되기까지의 시차 모니터링  <br>- **가십 프로토콜 (Gossip)**: 분산 피어 노드 간의 상태 정보(Heartbeat)를 Peer-to-Peer로 무작위 공유하여 생존 여부 실시간 전파|

---

### **관점 5️⃣ 분산 보안 경계·규제 준수·통신 신뢰성 비교 (How to Secure)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**노드 간 통신 보안**|노드와 노드 사이의 무선/유선 데이터 이동을 어떻게 보호하는가|- **mTLS (Mutual TLS)**: 분산 클러스터 내부 노드 간 통신 시 상호 인증서를 검증하여 비인가 노드의 무단 가입 및 도청 방지  <br>- **망 격리**: 데이터베이스 노드 간 통신은 프라이빗 서브넷(Private Subnet) 및 VPC 내부 통신으로 한정하여 외부 노출 원천 차단|
|**데이터 주권 규제**|글로벌 분산 환경에서 국가별 법령을 어떻게 준수하는가|- **데이터 레지던시**: EU GDPR 법령 등에 의거하여 특정 국가 고객 정보는 물리적으로 해당 국가 내에 위치한 샤드 노드에만 보관  <br>- **비식별화 샤딩**: 마스터 키 정보와 식별 개인 정보가 포함된 컬럼 데이터를 물리적으로 서로 다른 분산 노드에 격리 저장 관리|
|**다중 마스터 충돌 해결**|이종 노드에서 동시 수정 시 데이터 무결성을 어떻게 수호하는가|- **CRDT**: 수학적인 연산 병합 법칙을 통해 중앙 충돌 해결자 없이 각 노드가 자율적으로 일관된 최종 합의 상태로 수렴하는 기술  <br>- **LWW (Last-Write-Wins)**: 동일 키 동시 변경 시 타임스탬프가 최종인 데이터만 덮어씀(TrueTime 등 원자시계/NTP 시간 동기화 필수)|
|**일관된 분산 백업**|재해 발생 시 여러 노드의 백업 정합성을 어떻게 보장하는가|- **분산 스냅샷 (Consistent Snapshot)**: 여러 분산 노드의 특정 상태 이미지를 일시 동결하거나 스냅샷 ID를 동기화하여 백업  <br>- **PITR (Point-in-Time Recovery)**: 모든 분산 노드의 증분 트랜잭션 로그를 시계열 정렬하여 특정 일관된 시점 상태로 정밀 롤백|

---

### **📋 분산 데이터베이스 핵심 기술 전체 비교 요약표**

|**비교 항목**|**CAP 이론 (CP vs AP)**|**분산 합의 (Raft/Paxos)**|**분산 트랜잭션 (2PC)**|**샤딩 (Sharding)**|
|---|---|---|---|---|
|**관련 영역**|분산 아키텍처 설계 방향성|분산 기지국/노드 상태 합의|데이터 무결성 및 원자성 제어|물리적 데이터 분산 저장|
|**핵심 역할**|설계 단계의 일관성 vs 가용성 결정|분산 리더 선출 및 동기화 무결성|다중 노드 간의 All-or-Nothing 보장|단일 데이터베이스 용량 한계 극복|
|**동작 메커니즘**|네트워크 분할 상황에 따른 제어 정책|과반수(Quorum) 수락 응답 합의|조정자 중심의 2단계 커밋(준비-완료)|파티션/샤드 키 해시 기반 분산 라우팅|
|**성능 영향도**|CP 선택 시 대기 시간 증가, AP는 정합성 저하|과반 합의 네트워크 RTT에 의한 지연|모든 노드 락 점유로 병목 유발 🚨|단일 노드 스캔 시 병목 해소, 고성능 구현 ✅|
|**보안/제약 연계**|강한 일관성으로 보안 정합성 수호|mTLS 기반 노드 간 보안 합의 권장|암호화 트랜잭션 세션 보장|개인정보 노드 격리 보관(규제 대응)|
|**핵심 한계**|네트워크 장애(P) 발생 시 한쪽 기능 포기|짝수 노드 구성 시 과반 합의 불능 위험|조정자 장애 시 참여 노드 무한 대기 (Blocking)|스키마 구조 변경 난해, 조인 연산 제약|
|**대표 적용 시나리오**|은행 결제(CP) vs SNS 뉴스피드(AP)|분산 DB 메인 리더 선출 알고리즘|분산 DB 간 계좌 이체 처리|대규모 사용자 로그 테이블 분산 저장|
|**주요 평가 지표**|일관성 응답 시차, 서비스 다운타임|리더 선출 시간, 합의 처리 속도(RTT)|트랜잭션 성공률, 블로킹 락 대기 시간|데이터 균등 분배율, 가상 노드 수|

### **관점 1️⃣ 데이터 모델 분류·저장 및 아키텍처 메커니즘 비교 (How it Stores)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**데이터 저장 모델**|NoSQL 데이터베이스가 데이터를 어떤 구조적 형식으로 보관하는가|- **Key-Value Model**: 고유 키와 바이트 어레이 형태의 값(Value)을 매핑하여 초고속 조회 구현(Redis)  <br>- **Document Model**: JSON/BSON 등 계층적 구조의 도큐먼트 형식으로 저장하여 조인 없는 구조 매핑(MongoDB)  <br>- **Wide-Column Model**: 행마다 열(Column)의 개수와 이름이 다를 수 있는 스파스(Sparse)한 컬럼 패밀리 구조(Cassandra)  <br>- **Graph Model**: 개체(Node)와 관계(Edge)를 포인터 구조로 연결해 네트워크망 데이터를 직접 매핑(Neo4j)|
|**저장 엔진 아키텍처**|물리 디스크와 메모리에 쓰기 작업을 어떻게 가속화하는가|- **LSM-Tree**: 쓰기를 메모리(MemTable)에 먼저 순차 기록하고, 백그라운드 컴팩션을 통해 불변 파일(SSTable)로 병합  <br>- **SSTable**: 디스크에 정렬 및 압축되어 저장된 불변(Immutable) 상태의 데이터 파트 포맷 파일  <br>- **Commit Log / WAL**: 메모리 데이터 유실에 대비하여 쓰기 변경 사항을 순차적으로 우선 기록하는 디스크 로그|
|**스키마 유연성**|데이터 구조 변화에 어떻게 대응하는가|- **Schema-less**: 고정된 스키마가 없어 애플리케이션 요구 조건에 맞추어 데이터 컬럼 및 형식을 동적 추가/수정 가능  <br>- **JSON/BSON 직렬화**: 하위 도큐먼트 중첩(Embedded) 및 배열 데이터를 조인 없이 단일 객체 안에 직렬화하여 영속화  <br>- **Index-free Adjacency (인접 목록)**: 그래프 탐색 시 전역 인덱스를 타지 않고 인접 노드의 포인터를 타고 직접 추적|
|**분산 및 조율**|다중 노드 환경에서 클러스터를 어떻게 관리하는가|- **가십 프로토콜**: 마스터 노드가 없는 Shared Nothing 토폴로지에서 피어 노드 간 상태(Gossip)를 P2P로 전파(Cassandra)  <br>- **Replica Set**: 단일 마스터가 쓰기를 처리하고, 슬레이브들이 합의 프로토콜(Raft 등)을 통해 다음 리더를 선출(MongoDB)|

---

### **관점 2️⃣ NoSQL 성능·동시성 및 대용량 가용성 평가 비교 (How well it Performs)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**속도 및 성능 비교**|대용량 입출력 상황에서 어떤 성능 효율을 보이는가|- **락 프리 (Lock-Free) 구조**: RDBMS의 무거운 테이블/행 락을 없애고 단말 간 동시 무등급 쓰기(Append-only) 속도 보장  <br>- **인메모리 레이턴시**: 데이터 전체를 디스크가 아닌 메모리에 적재하여 마이크로초(µs) 단위의 응답 성능 실현(Redis)  <br>- **순차 I/O**: LSM-Tree 아키텍처를 적용해 무작위 디스크 탐색 헤더 이동을 없애고 쓰기 성능을 극대화|
|**일관성 및 분산**|데이터 합의 일관성과 속도 간 균형은 어떠한가|- **BASE 원칙**: Basically Available(기본 가용성), Soft State(유동적 상태), Eventual Consistency(최종 일관성) 지향  <br>- **Replication Lag**: 마스터 노드의 데이터가 복제 노드로 전달되어 반영 완료되기까지의 비동기 시차  <br>- **세션 일관성**: 최종 일관성 모델 하에서도 특정 클라이언트 세션이 본인이 쓴 데이터는 즉시 읽을 수 있게 보장|
|**자원 임계 및 병목**|디스크 정돈 시 어떤 자원 임계가 발생하는가|- **Compaction 오버헤드**: 흩어진 SSTable들을 병합할 때 발생하는 급격한 디스크 I/O 병목 및 임시 파일 공간 부족 현상  <br>- **JVM 가비지 컬렉션 (GC)**: 자바 기반 NoSQL(HBase, Cassandra)에서 발생하는 Stop-the-world 일시 시스템 중단 지연  <br>- **메모리 단편화**: 인메모리 DB 가동 시 메모리 할당 및 해제 반복으로 실물 가용 메모리가 고갈되는 단편화율 증가|
|**장애 복구 가용성**|장애 시 단물 복구 및 서비스 유지는 어떠한가|- **Replica Set 자동 합의**: 액티브 리더(Master) 노드가 다운되었을 때 수 초 이내 다른 세컨더리들이 새로운 리더를 선출  <br>- **마스터리스 가용성**: 중앙 리더가 없으므로 임의의 노드가 사망하더라도 다른 가십 노드가 무중단 쓰기를 즉각 연속 수행|

---

### **관점 3️⃣ NoSQL 데이터 모델별 최적 환경·적용 조건 비교 (When to Use)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**NoSQL 최적 태스크**|어떤 데이터 시나리오에 각 NoSQL 기술이 가장 적합한가|- **Key-Value**: 웹 서비스 세션 스토어, 고성능 데이터 캐싱 레이어, 메시지 큐(Pub/Sub) 기능 구현  <br>- **Document**: 유연한 속성이 요구되는 모바일 앱 메타데이터, 카탈로그, 사용자 프로필, 콘텐츠 관리 시스템  <br>- **Wide-Column**: IoT 스마트 그리드 센서의 수조 건 단위 시계열 데이터 적재, 대규모 분산 기기 로그 집계 수집  <br>- **Graph**: 지능형 이상금융거래 탐지(FDS), 소셜 네트워크 인맥 상관관계 분석, 추천 알고리즘의 지식 그래프 구축|
|**CAP 기반 모델 매핑**|일관성과 가용성 대조 전략|- **CP System (MongoDB, Redis)**: 단절 시 일관성을 최우선으로 수호(리더 선출 시간 동안 쓰기 차단으로 일시적 가용성 상실)  <br>- **AP System (Cassandra)**: 데이터 정합성이 깨지더라도 가용성을 수호(장애 노드가 있어도 정상 노드로의 쓰기를 항상 수락)|
|**역정규화 및 모델 설계**|조회 쿼리 설계 시 RDBMS와의 아키텍처 차이|- **역정규화 (Denormalization)**: 조인(Join) 연산이 불가능하므로, 조회 시 필요한 연산 결과를 사전에 단일 컬럼/문서에 내장  <br>- **임베딩 vs 참조**: 1:N 관계 정의 시 자식 데이터를 한 문서에 포함시킬 것인가(Embedding) vs 식별 키로 링크할 것인가(Reference)|
|**구조적 한계·위험**|각 NoSQL 기술 도입 시 노출되는 한계 및 사이드 이펙트는 무엇인가|- **Ad-hoc 질의 한계**: 복잡한 다중 테이블 조인, 서브쿼리 등의 관계형 쿼리 구현이 불가능(애플리케이션 단에서 구현)  <br>- **일관성 결여 위험**: 원자적 트랜잭션(ACID) 제어가 제한적이므로, 잔액 인출 등 금융 정합성 중시 데이터에 오작동 유발 리스크  <br>- **인덱스 비대화**: 스키마리스 특징으로 컬럼이 파편화되어 생성된 인덱스 크기가 실제 물리 RAM을 초과할 때 조회 성능 급락|

---

### **관점 4️⃣ NoSQL 데이터 최적화·인메모리 및 분산 운영 비교 (How to Optimize)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**인메모리 지속성**|메모리 전용 NoSQL의 자원 낭비를 어떻게 개선하는가|- **AOF vs RDB**: 매 쓰기마다 로그를 쓰는 AOF(정합성 높음)와 주기적 스냅샷을 뜨는 RDB(빠름) 옵션 간의 디스크 I/O 최적화  <br>- **Maxmemory Policy**: 메모리 임계 초과 시 오래된 키를 방출하는 알고리즘 최적화(LRU, LFU, Volatile-ttl 등 선택)  <br>- **메모리 단편화 개선**: 커스텀 메모리 할당자(Jemalloc 등) 옵션 튜닝을 통해 운영체제와의 메모리 회수 속도 제어|
|**컴팩션 튜닝**|디스크 정돈 오버헤드를 어떻게 최적화하는가|- **Size-tiered Compaction**: 크기가 유사한 SSTable을 모아 병합(쓰기 성능 향상에 좋으나 일시 디스크 용량 2배 필요)  <br>- **Leveled Compaction**: 레벨별로 중복 키가 없는 고정 크기 SSTable을 병합(읽기 성능 최적화, 디스크 I/O 오버헤드 큼)|
|**샤딩 및 로드 밸런싱**|대용량 NoSQL 클러스터링을 어떻게 최적화하는가|- **파티션 키 분산 튜닝**: 고유성이 높은 ID 값을 주 키로 삼아 데이터가 특정 물리 노드에 쏠리는 현상(Data Skew) 방지  <br>- **가상 노드 조율**: 물리 서버의 스펙에 맞추어 가상 노드 할당 비율을 조정하여 주소 공간 링(Ring) 상의 데이터 균등 적재|
|**일관성 수준 세부 튜닝**|프로토콜 레벨에서 일관성과 속도를 어떻게 세부 조율하는가|- **W/R 정합성 튜닝**: `Consistency Level = ALL` (모든 노드 반영, 속도 최저/정합성 최고), `ONE` (1개 노드 반영, 속도 최고)  <br>- **Local Quorum**: 다중 리전 환경에서 특정 로컬 리전 내부 노드 간의 과반수 합의만 거치도록 통신 레이턴시 단축|

---

### **5️⃣ 데이터 보안·보안 규제 준수·통신 신뢰성 비교 (How to Secure)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**보안 인증 및 ACL 통제**|NoSQL에 대한 비인가 접근을 어떻게 방어하는가|- **분산 인증**: Kerberos/SASL 프레임워크를 적용하여 클라이언트와 노드, 혹은 노드 간 통신 시 강력한 암호화 신원 인증 강제  <br>- **필드/도큐먼트 권한 제어**: JSON 도큐먼트 내 특정 노드나 민감한 필드 영역에 대해 사용자 그룹별 읽기/쓰기 권한 분할 지정  <br>- **보안 포트 설정**: 기본 탑재 포트 변경 및 접속용 비밀번호 복잡성 설정(비밀번호 없는 디폴트 세팅 상태 외부 접근 제한)|
|**전송 구간 암호화**|노드 간의 데이터 이동 시 정보 유출을 어떻게 방지하는가|- **TLS/SSL 터널링**: 노드 간 데이터 동기화 통신(Inter-node), 클라이언트의 질의 요청 통신(Client-to-node) 전 구간 암호화  <br>- **서브넷 망 격리**: 클러스터 노드들 간의 가십 프로토콜 통신망은 퍼블릭에 노출하지 않고 오직 사설 VPC 서브넷 내부로 통제|
|**암호화 규제 준수**|개인정보 보호 규정을 NoSQL에서 어떻게 충족하는가|- **필드 레벨 암호화 (FLE)**: 주민번호 등 고유식별정보 컬럼 데이터를 소스코드 단에서 암호화한 후 JSON 데이터로 저장  <br>- **저장 데이터 암호화**: 클러스터가 기록되는 물리 디스크 드라이브 자체를 암호화하는 스토리지 암호화 적용(TDE 준수)|
|**장애 복구 이미지 백업**|재해 발생 시 NoSQL의 데이터를 어떻게 일관되게 백업하고 복구하는가|- **복제 셋 무결성**: 인위적인 리더 탈취 침해 공격 시 자율 리더 선출 알고리즘이 정상 격리 및 마스터 재구성하는지 감사 검증  <br>- **일관성 있는 복제본 스냅샷**: 분산된 SSTable 데이터의 Snapshot을 무중단 백업하고 외부 격리된 보안 스토리지에 이송|

---

### **📋 NoSQL 데이터베이스 핵심 기술 전체 비교 요약표**

|**비교 항목**|**Key-Value (인메모리)**|**Document (도큐먼트)**|**Wide-Column (열 지향)**|**Graph DB (그래프)**|
|---|---|---|---|---|
|**핵심 데이터 구조**|키(Key) - 값(Value) 쌍|JSON / BSON 계층 문서|Sparse 컬럼 패밀리 구조|노드(Node) - 관계(Edge) 포인터|
|**주요 스키마 속성**|완전히 스키마리스 (Schema-less)|스키마리스 (유연한 구조 가능)|스키마리스 (테이블 단위 컬럼 정의)|스키마리스 (관계 속성 정의 유연)|
|**주요 L4 기반 통신**|TCP|TCP|TCP / UDP (가십 프로토콜 연동)|TCP|
|**일관성 기본 모델**|강한 일관성 (인메모리 기준)|강한 일관성 (Replica Set 기준)|최종 일관성 (Eventual) ✅|강한 일관성 (ACID 준수 지향)|
|**수평 확장 아키텍처**|샤딩, Redis Cluster 지원|Sharded Cluster, 샤딩 라우터|Masterless, Consistent Hash 링 ✅|분산 확장 복잡성 높음 (단일 스택 선호)|
|**주요 한계**|값 검색 제한(오직 키로만 탐색)|대용량 조인 연산 불가능|조인 불가, 수많은 인덱스 비대화|대규모 노드 분산 시 데이터 파편화|
|**대표 적용 분야**|고성능 캐싱, 세션 저장, 메신저|스마트폰 프로필, 카탈로그, 웹서버|IoT 시계열 데이터, 대용량 로그 수집|이상 금융 거래 탐지, 소셜 인맥 추천|
|**핵심 평가지표**|TPS, Read/Write Latency (µs)|Document 생성 속도, 인덱스 힛율|컴팩션 소요시간, 디스크 I/O 비율|깊이(Depth) 탐색 시간, 초당 경로 스캔|


### **관점 1️⃣ 데이터 모델링 단계·추상화 레벨 및 표현 메커니즘 비교 (How it Models)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**모델링 단계**|비즈니스 요구사항을 데이터베이스로 어떻게 구체화하는가|- **개념 모델링**: 비즈니스의 주요 핵심 개체와 관계를 식별하는 최고 수준의 추상화 단계(ERD 도출)  <br>- **논리 모델링**: 상세 속성을 정의하고, 1~3차 정규화를 수행하며, M:N 관계를 완전히 해소하는 기술 독립적 설계  <br>- **물리 모델링**: 특정 DBMS 스펙에 맞춰 데이터 타입 결정, 인덱스 생성, 테이블스페이스 및 파티션 물리 설계|
|**엔티티 관계 매핑**|개체 간의 연결 구조를 어떻게 논리적으로 정의하는가|- **식별 관계**: 부모 테이블의 기본키(PK)가 자식 테이블의 기본키(PK) 영역으로 그대로 상속되는 강한 의존 관계  <br>- **비식별 관계**: 부모의 기본키가 자식의 일반 속성(FK)으로 상속되어 자식이 부모 없이도 생존 가능한 관계  <br>- **M:N 관계 해소**: 다대다 관계를 1:N 및 N:1 관계로 분해하기 위해 두 엔티티 사이에 배치하는 **교차 엔티티**|
|**상관 분석 검증**|업무 프로세스와 데이터 모델 간 정합성을 어떻게 검증하는가|- **CRUD Matrix**: 업무 프로세스(C, R, U, D)와 엔티티 간의 매핑을 통해 누락되거나 왜곡된 데이터 흐름 교차 검증  <br>- **상관 다이어그램**: 데이터의 생성처와 소비처를 매핑해 비즈니스 프로세스와 데이터 스키마 간 일치성 검증|
|**식별자 선정 전략**|엔티티의 유일성을 식별하기 위해 어떤 키를 선택하는가|- **주 식별자**: 엔티티를 대표하며 유일성, 최소성, 불변성, 존재성을 완벽히 충족하는 고유 키(Natural Key)  <br>- **인조 식별자 (Surrogate Key)**: 비즈니스적 주식별자가 너무 길거나 복잡하여 인위적으로 생성한 가상의 일련번호 식별자  <br>- **대체 식별자**: 주식별자를 대신해 물리 스키마상에서 고유성(Unique)을 보증할 수 있는 보조 키|

---

### **관점 2️⃣ 데이터 정규화·정합성 검증 및 품질 평가 비교 (How well it Structures)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**정규화 단계 제어**|데이터 중복을 제거하기 위해 데이터 모델을 어떻게 구조화하는가|- **1정규화 (1NF)**: 다중값 또는 반복 그룹 속성을 분리하여 컬럼의 도메인을 단일 원자값(Atomic Value)으로 분해  <br>- **2정규화 (2NF)**: 기본키가 복합키인 경우, 기본키 일부에 종속되는 부분 함수 종속성 제거(완전 함수 종속화)  <br>- **3정규화 (3NF)**: 기본키 이외의 일반 속성 간에 발생하는 이행적 함수 종속성(X→Y→ZX→Y→Z) 제거|
|**고급 정규화**|복잡한 데이터 종속성에 따른 이상 현상을 어떻게 제어하는가|- **BCNF**: 결정자이면서 동시에 후보키가 아닌 함수 종속성을 분해하여 결정자가 무조건 후보키가 되도록 수정  <br>- **4정규화 & 5정규화**: 다중값 종속성(MVD)을 제거하는 4NF, 조인 종속성(JD)을 분해하여 무손실 조인 보증하는 5NF  <br>- **이상 현상 (Anomaly)**: 정규화를 거치지 않은 중복 설계로 발생하는 삽입 이상, 삭제 이상, 갱신 이상 현상 방지|
|**정합성 무결성 평가**|설계된 데이터 모델의 건강성을 어떻게 정량화하는가|- **데이터 품질 관리 지표**: 데이터의 완전성, 유일성, 정확성, 일관성을 수치화하여 데이터 표준 준수 수준 평가  <br>- **도메인 준수율**: 스키마에 정의된 도메인 규칙(타입, 크기, 제약조건)을 벗어나는 예외 데이터 비율 측정  <br>- **참조 무결성 오류율**: 부모 테이블에 없는 기본키를 참조하는 고아 외래키(Orphan FK)의 물리적 존재 수준 검사|
|**물리 성능 병목 평가**|설계된 모델이 런타임에 성능에 미칠 영향을 어떻게 판단하는가|- **조인 뎁스 (Join Depth)**: 데이터 조회 시 한 번에 연결해야 하는 테이블의 최대 개수(과도 시 옵티마이저 하드 파싱 유발)  <br>- **Null 허용 비율**: 테이블 내에 가변 컬럼 및 Null 값이 차지하는 비율(불필요한 디스크 블록 낭비 유발 척도)|

---

### **관점 3️⃣ 스키마 물리 설계·기술별 최적 환경 및 적용 조건 비교 (When to Use)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**모델링 최적 태스크**|어떤 데이터 처리 비즈니스 시나리오에 각 모델링 기법이 가장 적합한가|- **개념/논리 모델링 (ERD)**: OLTP 시스템 등 고도의 데이터 무결성 보장과 비즈니스 정합성이 최우선인 관계형 데이터 설계  <br>- **다차원 모델링 (스타 스키마)**: 데이터웨어하우스(DW), 대규모 BI 분석 환경의 팩트(Fact) 및 차원(Dimension) 고속 통계 조회  <br>- **스키마리스 모델링**: 스키마 정의 없이 빠르게 비정형 로그, 문서 메타데이터를 저장 및 Scale-Out 하는 NoSQL 스키마 설계|
|**논리 모델 vs 물리 모델**|두 모델링 기법의 핵심 차이|- **논리 데이터 모델**: 하드웨어/DBMS 독립성 유지, 정규화 지향, 한글 속성 명명, 업무 비즈니스 로직 중심 정의  <br>- **물리 데이터 모델**: 특정 DBMS 스펙 종속 허용, 반정규화 적극 고려, 영문 식별자 매핑, 물리 데이터 타입 구체화|
|**반정규화 기법 선택**|성능 최적화를 위해 어떤 설계 조율을 수행하는가|- **테이블 반정규화**: 조인 횟수를 줄이기 위한 테이블 통합(1:1, 1:N), 물리 디스크 분리를 위한 테이블 분할(수직/수평)  <br>- **컬럼/관계 반정규화**: 계산/중복 컬럼 배치, 이력 테이블의 최신 플래그 컬럼 추가, 1:N 조인 생략을 위한 중복 관계 연결|
|**한계·위험**|각 모델링 및 정규화 기법 도입 시 발생하는 구조적 리스크는 무엇인가|- **과도한 정규화의 함정**: 릴레이션이 너무 쪼개져 조회 시 조인 연산이 급증하여 런타임 CPU 및 I/O 성능 급락  <br>- **무분별한 반정규화 위험**: 데이터 중복 적재로 데이터 변경 시 갱신 오버헤드 증가 및 데이터 불일치 정합성 오류 유발  <br>- **인조 식별자 남용**: 복합 기본키 복잡성을 피하려고 인조 키를 모든 테이블에 남발하여 관계 가독성 및 정체성 손실|

---

### **관점 4️⃣ 논리/물리 데이터 모델 최적화 및 메타데이터 운영 비교 (How to Optimize)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**표준화 및 메타데이터**|전사적인 데이터 일관성을 위해 어떤 최적화 기법을 적용하는가|- **표준 단어/용어 사전**: 컬럼 이름 파편화와 의미 왜곡을 차단하기 위한 명명 규칙(Naming Rule) 및 표준 도메인 정의  <br>- **메타데이터 관리 시스템 (MDS)**: 전사적 물리/논리 스키마 정보와 표준 사전을 통합 관리하여 데이터 변경 영향도 분석 지원  <br>- **데이터 계보 (Data Lineage)**: 데이터의 생성지부터 최종 분석 리포트 소비지까지의 데이터 흐름 경로 시각화 감시|
|**물리적 저장소 최적화**|데이터베이스 저장 효율을 극대화하기 위해 무엇을 설계하는가|- **테이블스페이스 분리**: 쓰기 부하가 높은 테이블과 인덱스를 물리적으로 서로 다른 디스크 저장 공간으로 분산 아키텍처 설계  <br>- **블록 프리 스페이스 (PCTFREE)**: 향후 테이블 갱신 시 발생하는 페이지 스플릿을 차단하기 위한 물리 페이지 여유 공간 비율 설계|
|**파티셔닝 설계**|대용량 데이터를 저장 레벨에서 어떻게 세분화 튜닝하는가|- **샤드/파티션 키 설계**: 범위(Range), 해시(Hash), 리스트(List) 분할 기준을 논리 모델 분석 단계에서 주 키와 매핑 설계  <br>- **파티션 프루닝 활성화**: 쿼리 튜닝 시 검색 조인 조건절에 반드시 파티션 키가 선행으로 인덱싱되도록 물리 컬럼 순서 지정|
|**CRUD 기반 프로세스**|설계된 모델과 트랜잭션의 정합성을 어떻게 최적화하는가|- **트랜잭션 분석도**: CRUD 매트릭스를 기반으로 특정 엔티티에 유입되는 초당 처리량을 분석해 병목 테이블 식별  <br>- **동시성 튜닝**: 쓰기 빈도가 매우 높은 엔티티는 공유 잠금(S-Lock) 충돌을 예방하기 위해 물리 설계 시 이력 테이블 구조로 재정립|

---

### **관점 5️⃣ 데이터 모델 보안·무결성 규제 준수 및 거버넌스 비교 (How to Secure)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**개인정보 격리 설계**|데이터 보안을 위해 스키마 레벨에서 어떻게 안전하게 격리하는가|- **민감 컬럼 분리**: 주민번호, 패스워드 등 고도의 기밀이 요구되는 속성은 별도의 1:1 관계의 테이블로 분리해 물리적 격리  <br>- **암호화 바이트 오버헤드**: 컬럼 암호화 시 평문 대비 데이터 크기 증가 현상(Base64 인코딩)을 대비해 물리 컬럼 바이트 확장 설계|
|**무결성 강제 참조 통제**|잘못된 데이터 유입을 스키마 레벨에서 어떻게 원천 차단하는가|- **Check 제약조건**: 도메인 범위 이외의 추가적인 도메인 제약(예: `gender IN ('M', 'F')`)을 스키마 규칙으로 영구 강제  <br>- **FK(외래키) 비활성화 규칙**: 무결성은 완벽히 보장되나, 실시간 대량 배치 처리 시 인덱스 경합을 막고자 FK 제어를 애플리케이션 단에 위임|
|**데이터 소유권 규제 준수**|데이터 주권과 글로벌 컴플라이언스를 설계에 어떻게 반영하는가|- **개인정보 파기 생명주기**: 개인정보보호법에 의거 회원 탈퇴 후 일정 기간 보존 기한이 만료되면 자동 삭제/비식별화되도록 생명주기 컬럼 설계  <br>- **데이터 레지던시 파티셔닝**: 특정 국가 시민 데이터를 해당 국가 서버에 보존하는 법 준수를 위해 국가 코드별 파티션 격리 설계|
|**거버넌스 및 감사 이력**|데이터 관리 책임을 명확히 하기 위해 무엇을 모델링하는가|- **공통 변경 이력 컬럼**: 모든 주요 엔티티 테이블에 등록일시, 수정일시, 등록자 ID, 수정자 ID 등 이력 감사(Audit) 속성을 필수 강제화  <br>- **데이터 아키텍트 (DA)**: 논리 모델 및 물리 변경 적용 이전에 표준 준수 검사 및 승인 흐름을 통제하는 데이터 거버넌스 체계 가동|

---

### **📋 데이터 모델링 핵심 기법 전체 비교 요약표**

|**비교 항목**|**개념 모델링**|**논리 모델링**|**물리 모델링**|**반정규화 (물리적 역정규화)**|
|---|---|---|---|---|
|**모델링 주 타깃**|비즈니스 요구사항 핵심 개체 추출|상세 데이터 구조 정의 및 정화|특정 DBMS 사양 최적화 물리 설계|런타임 쿼리 조회 성능 병목 개선|
|**추상화 레벨**|가장 높음 (이론적/비즈니스 중심)|중간 (기술 독립적 정적 구조 설계)|낮음 (물리 디스크 설계 연계)|낮음 (물리 성능 튜닝 직결)|
|**주요 핵심 산출물**|주제 영역 정의서, 개략적 ERD|상세 ERD, 정규화 명세서, 관계 정의|테이블 정의서, DDL 스크립트, 파티션|반정규화 튜닝 설계서, 테이블 분할 이력|
|**데이터 정합성 보장**|개괄적인 정합성 검토 수준|정규화를 통해 데이터 중복 완전 배제 ✅|제약조건 물리 정의를 통해 보장|데이터 중복 허용으로 **정합성 훼손 위험** 🚨|
|**성능 오버헤드**|없음 (물리적 기동 영역 없음)|없음 (이론적 구조 설계 단계)|인덱스/파티션 부재 시 Full Scan 유발|쓰기/수정 시 데이터 갱신 오버헤드 증가|
|**핵심 한계**|상세 비즈니스 규칙 표현 불가|실제 DBMS 반영 시 성능 저하(조인)|설계 변경 시 전체 DDL 재생성 오버헤드|데이터 불일치에 의한 삽입/삭제 이상|
|**대표 적용 시나리오**|신규 차세대 프로젝트 초기 분석 단계|비즈니스 업무 엔티티 상세 속성 정의|Oracle, MySQL 등 대상 DBMS 타겟 설계|조인 연산 폭증으로 홈페이지 지연 시 튜닝|
|**주요 평가 지표**|핵심 엔티티 도출률, 요구사항 커버리지|3정규화 달성률, M:N 관계 해소율|파티션 크기 적정성, 인덱스 튜닝 지수|데이터 불일치 발생 건수, 조인 비용 감소율|


### **관점 1️⃣ 빅데이터·DW 아키텍처·데이터 결합 및 저장 메커니즘 비교 (How it Stores)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**분석 인프라**|대규모 데이터를 수집하고 분석하기 위해 어떤 저장 아키텍처를 구성하는가|- **데이터 웨어하우스 (DW)**: 구조화된 정형 데이터를 주제 영역별로 통합하여 비즈니스 분석(BI)에 최적화된 고성능 저장소  <br>- **데이터 레이크 (Data Lake)**: 정형, 반정형, 비정형 원본 데이터(Raw Data)를 가공 없이 오브젝트 스토리지에 무제한 보관  <br>- **데이터 레이크하우스**: 데이터 레이크의 저비용 저장 구조 위에 DW의 트랜잭션 정합성(ACID)과 스키마 관리를 결합|
|**다차원 모델링**|분석을 위해 데이터를 어떻게 다차원적으로 모델링하는가|- **스타 스키마**: 중심의 팩트 테이블(Fact)에 차원 테이블(Dimension)이 직접 1단계 조인 구조로 매핑(조회 성능 최고)  <br>- **스노우플레이크**: 차원 테이블을 3정규화하여 테이블 조인 오버헤드가 발생하나 중복을 최소화하여 저장소 최적화  <br>- **정형/반정형/비정형**: 테이블 행/열 규격 데이터(RDB) vs JSON/BSON(NoSQL) vs 파일/이미지/텍스트 원본 데이터|
|**분산 파일 포맷**|대량 데이터를 분산 디스크에 어떻게 저장하고 압축하는가|- **HDFS**: 블록 단위로 데이터를 분산 보관하고 3중 복제본을 유지해 유실에 대응하는 하둡 기반 분산 파일 시스템  <br>- **Parquet / ORC**: 분석 쿼리에 필요한 특정 열(Column)만 선별 스캔하는 열 지향 포맷(디스크 I/O 절감, 고압축율)  <br>- **델타 레이크**: Parquet 파일 아키텍처 상단에 ACID 트랜잭션 로그를 구현하여 데이터 레이크의 무결성 보장|
|**수집 및 적재**|원천 데이터를 가공하여 분석계로 어떻게 이동시키는가|- **ETL**: 원천 데이터를 임시 영역에서 전처리/가공한 후 정형 스키마 구조의 DW에 최종 적재하는 전통적 방식  <br>- **ELT**: 원천 데이터 가공 없이 데이터 레이크에 먼저 고속 적재한 후, 분석 시점에 분산 컴퓨팅으로 변환하는 방식  <br>- **CDC (Change Data Capture)**: 데이터 소스(RDBMS)의 트랜잭션 로그를 추적하여 실시간 변경 데이터만 추출 적재|

---

### **관점 2️⃣ 분산 처리 성능·실시간 분석 속도 및 가용성 평가 비교 (How well it Processes)**

| **구성요소**     | **핵심 내용**                          | **세부 키워드**                                                                                                                                                                                                                                                 |
| ------------ | ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **분산 연산 속도** | 수조 건의 데이터 연산을 어떻게 병렬화하여 가속하는가      | - **MapReduce**: 데이터를 매핑(Map)하고 합쳐(Reduce) 분석하는 하둡 기반 디스크 배치 연산 프레임워크(I/O 병목 유발)  <br>- **Apache Spark**: 메모리 내(In-Memory) 가상 데이터셋(RDD/Dataframe) 연산으로 디스크 I/O를 없애 성능 100배 향상  <br>- **MPP (대규모 병렬 처리)**: 독립된 노드들이 독립적인 CPU/메모리로 쿼리를 동시 병렬 처리하는 분산 DW 아키텍처 |
| **실시간 스트리밍** | 실시간으로 흐르는 데이터를 유실 없이 처리하는 속도는 어떠한가 | - **스트림 처리**: 데이터가 생성되는 즉시 건별로 마이크로초(µs) 단위 가공(Apache Flink, Spark Structured Streaming)  <br>- **배치 처리**: 유입된 데이터를 대량으로 모아 매시간, 매일 등의 특정 스케줄 주기마다 디스크 일괄 연산 처리  <br>- **마이크로 배치**: 스트림 데이터를 수 초 이내의 짧은 윈도우 단위로 모아 고속 연속 처리하는 기법                           |
| **파이프라인 성능** | 대량 처리 시 병목 현상을 어떻게 해소하는가           | - **데이터 스케팅 (Skewness)**: 특정 분산 노드에 데이터 가중치가 쏠려 클러스터 전체 성능이 하향 평준화되는 병목 지연  <br>- **병렬 처리도 (DOP)**: 분산 클러스터의 코어 수에 맞추어 태스크 파티션 개수를 최적으로 할당하는 병렬 멀티스레딩 성능  <br>- **네트워크 셔플링**: 분산 노드 간에 조인 또는 정렬을 수행하기 위해 네트워크 카드를 타고 데이터가 교환되는 병목 제어                       |
| **장애 자가 복구** | 노드 장애 시 데이터 유실을 어떻게 예방하는가          | - **RDD Lineage (계보)**: Spark 연산 중 노드 유실 시, 연산 히스토리 계보를 추적하여 메모리 내 데이터를 자동 복구  <br>- **NameNode HA**: 하둡의 분산 파일 메타데이터를 관리하는 네임노드를 Active-Standby 이중화로 구성해 단일 장애점 극복                                                                                        |

---

### **관점 3️⃣ 빅데이터·DW 기술별 최적 환경 및 적용 조건 비교 (When to Use)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**빅데이터 최적 태스크**|어떤 비즈니스 분석 분석 환경에 각 기술이 가장 적합한가|- **데이터 웨어하우스 (DW)**: 기업 재무 제표, 연간 매출 마감 보고서, 고정된 표준 규격의 정형화된 비즈니스 대시보드  <br>- **데이터 레이크 (Data Lake)**: 머신러닝/딥러닝용 훈련 이미지 및 원본 텍스트 수집, 원천 로그 보존, AI 데이터 분석  <br>- **데이터 레이크하우스**: BI(비즈니스 인텔리전스) 보고서와 AI(인공지능) 학습 인프라를 하나로 결합한 단일 분석 환경 구축  <br>- **하둡 (Hadoop)**: 테라/페타바이트 단위의 장기 미사용 데이터 저비용 아카이빙 보관 및 초대형 디스크 배치 연산|
|**분석 기술 3대 비교**|DW vs Data Lake vs Lakehouse|- **스키마 바인딩**: DW(Schema-on-Write, 적재 시 정의) vs Data Lake(Schema-on-Read, 사용 시 정의) vs Lakehouse(ACID 기반)  <br>- **저장 비용**: Data Lake/Lakehouse(클라우드 오브젝트 스토리지 기반, 무한 확장, 최저 비용) >> DW(고성능 물리 인프라, 고비용)|
|**DW 모델링 비교**|스타 스키마와 스노우플레이크 스키마 매핑 차이|- **Star Schema**: 조인 경로가 최소화되어 쿼리 속도가 가장 빠름(차원 데이터 중복 설계로 디스크 용량 소모 감수)  <br>- **Snowflake Schema**: 차원 테이블 정규화로 저장 공간은 아끼나, 다중 조인(Multi-Join) 유발로 조회 응답 시간 저하|
|**구조적 한계·위험**|각 빅데이터 기술이 내포한 본질적인 한계와 위험 요인은 무엇인가|- **데이터 늪 (Data Swamp)**: 데이터 레이크 구축 후 용어 사전 및 접근 통제 관리 부재로 데이터를 쓸 수 없게 오염 방치하는 상태  <br>- **하둡 스몰 파일 문제**: HDFS 기본 블록 크기(128MB)보다 극도로 작은 파일 폭증 시 네임노드 메모리가 고사하는 치명적 장애  <br>- **셔플링 오버헤드**: 대량 분산 조인 시 노드 간 이더넷 트래픽이 폭증하여 시스템 전송이 마비되는 네트워크 보틀넥 현상|

---

### **4️⃣ 빅데이터 파이프라인 최적화·튜닝 및 운영 통제 비교 (How to Optimize)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**컬럼형 압축 최적화**|분석용 I/O 부하를 어떻게 최소화하는가|- **프레디케이트 푸시다운**: 쿼리 조건절 필터(WHERE)를 물리 파일(Parquet) 레벨에 직접 던져 불필요한 행 스캔 원천 차단  <br>- **파일 메타데이터 인덱스**: Parquet 내부의 Min/Max 통계 블록 정보를 이용하여 가치 없는 파일 블록은 스캔 스킵 처리|
|**분산 메모리 튜닝**|분산 엔진 가동 시 발생하는 메모리 낭비를 어떻게 개선하는가|- **브로드캐스트 조인**: 소량 테이블을 모든 분산 노드로 복제 전송해 대량 테이블 조인 시 발생하는 네트워크 셔플링 제거  <br>- **파티션 리디자인**: 데이터 리스트 크기를 고려하여 작업 파티션 수를 병렬 튜닝(`repartition`, `coalesce` 함수 적재 활용)|
|**스토리지 쓰기 최적화**|클라우드 환경에서 저장 비용과 속도를 어떻게 튜닝하는가|- **컴퓨팅-스토리지 분리**: 클라우드 가상 머신(EC2)과 오브젝트 스토리지(S3)를 분리해 분석 시에만 VM을 띄우는 비용 절약  <br>- **파일 컴팩션 (Compaction)**: 스트리밍 수집으로 잘게 쪼개진 로그 파일들을 주기적으로 256MB~512MB 크기 파일로 병합 튜닝|
|**워크플로우 오케스트레이션**|복잡한 빅데이터 파이프라인의 작업 흐름을 어떻게 관리하는가|- **Airflow 워크플로우**: 파이프라인 단계를 유향 비순환 그래프(DAG)로 설계하고 테스크 실패 시 자동 재시도(Retry) 통제  <br>- **백필 (Backfill)**: 파이프라인 장애 복구 후 유실되었던 과거 특정 날짜 구간의 데이터를 안전하게 소급 적재 자동화|

---

### **5️⃣ 데이터 보안·빅데이터 규제 준수 및 거버넌스 신뢰성 비교 (How to Secure)**

|**구성요소**|**핵심 내용**|**세부 키워드**|
|---|---|---|
|**암호화 및 비식별화**|수조 건의 데이터 보안 기밀성을 어떻게 확보하는가|- **토큰화 (Tokenization)**: 카드번호 등 중요 정보를 난수화된 대체 토큰으로 매핑하여 빅데이터 분석계 보관(유출 피해 차단)  <br>- **개인정보 비식별화**: 식별성이 높은 속성을 k-익명성, l-다양성, t-긴밀성 수학 모델에 따라 가명화/익명화 처리 후 적재|
|**분산 데이터 접근 통제**|누가 어떤 분석 데이터에 접근할 수 있는지 어떻게 제어하는가|- **Apache Ranger**: 하둡, 스파크, 하이브 전체의 컬럼/로우 레벨의 세분화된 접근 권한 및 마스킹 정책을 중앙 집중식 제어  <br>- **ABAC (속성 기반 접근 제어)**: 사용자의 부서, 접속 시간대, 단말 위치 속성을 바탕으로 동적 분석 테이블 접근 허용 제어|
|**글로벌 프라이버시 법률**|데이터 레이크의 규제 준수 가이드라인은 무엇인가|- **잊힐 권리 (GDPR)**: 고객 탈퇴 시 데이터 레이크의 Parquet/ORC 백업본에서 특정인 데이터를 지우기 위해 ACID 델타 레이크 적용  <br>- **데이터 주권**: 특정 국가(EU 등) 시민의 데이터가 자국 물리 영역 밖의 리전 노드로 유출되지 않도록 전송 차단 통제|
|**거버넌스 및 감사 이력**|데이터 늪을 방지하기 위해 어떤 관리 통제를 구축하는가|- **데이터 카탈로그**: 비즈니스 메타데이터 사전을 연동해 컬럼의 물리 명칭과 의미를 맵핑하고 비인가 테이블 생성 차단  <br>- **데이터 리니지 (Data Lineage)**: 원천 데이터 수집부터 요약 테이블 생성까지의 변환 이력 계보를 그래프로 추적 관리 및 감사|

---

### **📋 빅데이터 및 DW 핵심 기술 전체 비교 요약표**

|**비교 항목**|**데이터 웨어하우스 (DW)**|**데이터 레이크 (Data Lake)**|**데이터 레이크하우스**|**하둡 (HDFS/MapReduce)**|
|---|---|---|---|---|
|**동작/분석 계층**|L7 (정형 비즈니스 분석계)|L7 (비정형/Raw 데이터 적재)|L7 (하이브리드 분석/ML 통합)|L3/L4 (분산 파일 및 배치 연산)|
|**핵심 역할**|주제 영역별 통합 고성능 BI 분석|정형/비정형 로우 데이터 보존|레이크의 비용 강점 + DW 정합성|대용량 분산 파일 저장 및 일괄 처리|
|**일관성/무결성**|강한 일관성 (ACID 완전 보장)|최종 일관성 (가공 전 무결성 결여)|강한 일관성 (ACID 트랜잭션 구현) ✅|파일 수준의 불변성 및 블록 복제|
|**스키마 바인딩**|Schema-on-Write (적재 시 설계)|Schema-on-Read (질의 시 설계)|Schema-on-Read / Write 지원|Schema-on-Read (VCS/Hive 연동)|
|**인프라 저장 비용**|높음 (고성능 전용 스토리지)|낮음 (오브젝트 스토리지 활용)|낮음 (오브젝트 스토리지 활용) ✅|낮음 (일반 COTS 서버 디스크)|
|**핵심 한계**|비정형 데이터 처리 불가, 확장 비용|메타데이터 부재 시 데이터 늪 전락|컴퓨팅 연산 노드 비용 관리 리스크|스몰 파일 문제, 실시간 분석 불가능|
|**대표 적용 분야**|기업 재무/마감 지표 보고서|AI 학습용 데이터셋 구축, Raw 로그|단일 데이터 플랫폼 기반 BI + AI 융합|페타바이트 아카이빙 배치 처리|
|**주요 평가 지표**|동시 쿼리 속도, 튜닝 TPS|데이터 수집율, 스토리지 점유율|데이터 일관성 랙(Lag), CPU 활용도|네임노드 메모리 사용량, 배치 완료 시간|
