### **TCP 혼잡제어의 패러다임 전환: BBR 알고리즘**

***

#### 답안 전체 스토리 흐름 (목차)

```
Ⅰ. 개요 (왜 손실 기반 혼잡제어의 시대가 저무는가)
Ⅱ. BBR 핵심 원리 및 동작 단계
Ⅲ. 기존 알고리즘과의 비교 및 적용 체계
Ⅳ. 결론
```

포인트: 개요에서 \*\*"앞서 다룬 선택적 재전송 ARQ가 '오류를 효율적으로 복구'하는 신뢰성 메커니즘이라면, BBR(Bottleneck Bandwidth and Round-trip propagation time)은 '언제 혼잡이 시작되는가'를 근본적으로 다시 정의한 혼잡제어 알고리즘이다 — 기존 CUBIC·Reno 같은 손실 기반(Loss-based) 알고리즘이 '패킷 손실 = 혼잡 신호'로 간주해 버퍼가 가득 찰 때까지 무작정 전송량을 늘리다 손실 후에야 감속하는 반면, BBR은 2016년 구글이 제안한 대로 '대역폭(BtlBw)과 왕복 지연시간(RTprop)을 실시간 측정해 버퍼가 차기 전에 최적 전송률을 찾는' 모델 기반(Model-based) 접근으로, 앞서 다룬 CDN·클라우드 네트워크의 버퍼블로트(Bufferbloat) 문제를 해결하며 YouTube·구글 클라우드의 핵심 전송 계층 기술로 자리잡은 것"\*\*이라는 한 줄로 시작하면 전체 맥락이 드러납니다.

***

#### Ⅱ. BBR 핵심 원리 및 동작 단계

**가. 손실 기반 vs 모델 기반 패러다임 전환**

```
[기존 손실 기반 혼잡제어의 한계: 버퍼블로트]

CUBIC/Reno 동작:
  전송량 계속 증가 → 중간 라우터 버퍼 채움
  → 버퍼 가득 참 → 패킷 손실 발생
  → 그제서야 "혼잡이다!" 감속

문제:
  버퍼가 크면(Bufferbloat) 손실 늦게 발생
  → 그 사이 지연시간(RTT) 계속 증가 🚨
  → 손실 = 이미 한참 늦은 신호
  → 처리량 낮고 지연 큰 최악의 상태 지속

[BBR의 핵심 통찰]

진짜 최적 전송 지점 = 버퍼가 차기 직전
  (BtlBw 도달 + RTprop 최소 유지)

파이프 모델:
  BtlBw(병목 대역폭) × RTprop(왕복 전파 지연)
  = BDP(Bandwidth-Delay Product)
  = 최적 전송량(In-flight 데이터량)
```

***

**나. BBR 4단계 상태 머신**

```
[BBR 동작 4단계]

①Startup (시작)
  지수적으로 전송률 증가 (2/ln2 배율)
  전달률(Delivery Rate) 더 이상 안 늘 때까지
  = BtlBw 탐색 단계
       ↓
②Drain (배출)
  Startup에서 쌓인 초과 버퍼 큐 배출
  전송률을 일시적으로 낮춤
  큐 길이 = BDP 수준으로 정상화
       ↓
③ProbeBW (대역폭 탐색) ← 정상 운영 상태
  8단계 게인 사이클 순환
  (1.25배 상승→0.75배 하강→1.0배×6회 유지)
  주기적으로 여유 대역폭 있는지 탐색
       ↓
④ProbeRTT (RTT 탐색)
  10초마다 전송량 최소화(4개 패킷)
  버퍼 큐 완전 비움 → 순수 RTprop 재측정
  네트워크 상태 변화 지속 반영
```

***

**나. BBR 핵심 체계**

| **핵심 척도**   | **📊 측정 메커니즘 🚨**                                                                                                    | **🔑 전송률 결정 🚨**                                                                | **🏁 버퍼블로트 해소 💯**                                                                         |
| :---------- | :------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------ | :----------------------------------------------------------------------------------------- |
| **핵심 측정**   | **BtlBw(병목 대역폭)**: 전달률(Delivery Rate)의 최근 10 RTT 윈도우 최댓값 / **RTprop(왕복 전파 지연)**: 최근 10초 윈도우 최솟값 RTT / 두 값을 지속 실시간 갱신 | **BDP 기반 페이싱**: 전송량 = BtlBw × RTprop / 페이싱(Pacing)으로 버스트 전송 방지 / 균일한 간격으로 패킷 전송 | **손실을 혼잡 신호로 사용 안 함**: 무선망 손실에도 강건 / 버퍼 점유 최소화 → 큐잉 지연 최소화 / 앞서 다룬 **선택적 재전송 ARQ**와 독립적 계층 |
| **파라미터 조정** | ProbeBW 게인 사이클: 1.25→0.75→1.0×6 / 상승 시 여유 대역폭 확인 / 하강 시 과다 전송 버퍼 배출                                                  | **cwnd(혼잡 윈도우) 상한**: BDP의 약 2배로 설정 / 손실 발생해도 전송률 급감 안 함(CUBIC 대비)               | **공정성 이슈**: BBR과 CUBIC 혼재 시 BBR이 버퍼 더 많이 점유 가능 🚨 / BBRv2·v3에서 손실 신호 일부 반영해 개선             |

***

#### Ⅲ. 기존 알고리즘과의 비교 및 적용 체계

**가. 혼잡제어 알고리즘 비교**

| 비교 항목              | Reno               | CUBIC       | BBR                 |
| :----------------- | :----------------- | :---------- | :------------------ |
| **접근 방식**          | 손실 기반              | 손실 기반       | **모델 기반**           |
| **혼잡 신호**          | 패킷 손실              | 패킷 손실       | BtlBw·RTprop 측정     |
| **버퍼 활용**          | 버퍼 가득 채움 🚨        | 버퍼 가득 채움 🚨 | **BDP 수준만 사용** ✅    |
| **지연시간**           | 높음(Bufferbloat) 🚨 | 높음 🚨       | **낮음** ✅            |
| **무선망 손실 대응**      | 성능 급감 🚨           | 성능 급감 🚨    | **강건함** ✅           |
| **처리량(고손실 환경)**    | 낮음                 | 낮음          | **높음** ✅            |
| **공정성(동일 알고리즘 간)** | 안정적                | 안정적         | 상대적으로 불안정           |
| **표준화**            | RFC 5681           | Linux 기본값   | RFC 9002 관련 / 구글 주도 |

***

**나. 적용 환경별 비교**

| 적용 환경                | BBR 효과                | 비고              |
| :------------------- | :-------------------- | :-------------- |
| **장거리 고대역폭(LFN)**    | 처리량 대폭 향상             | 위성·대륙 간 회선      |
| **무선·모바일 네트워크**      | 손실 있어도 처리량 유지         | LTE·5G 환경       |
| **CDN·동영상 스트리밍**     | 지연시간 감소·버퍼링 감소        | YouTube 적용 사례   |
| **데이터센터 내부**         | BBRv2/v3로 공정성 개선 후 적용 | Google Cloud 적용 |
| **혼재 환경(BBR+CUBIC)** | 공정성 이슈 주의 필요          | v2/v3로 지속 개선 중  |

***

**다. 앞서 다룬 개념과의 연결**

| 연계 개념               | 연결 내용                                    |
| :------------------ | :--------------------------------------- |
| **선택적 재전송 ARQ**     | ARQ=오류 복구 계층 / BBR=전송률 결정 계층 / 상호 독립적 협력 |
| **QUIC**            | BBR을 기본 혼잡제어로 채택 / HTTP/3 성능 향상 핵심       |
| **NTN 위성 통신**       | 고지연 환경에서 BBR의 BDP 기반 전송이 처리량 극대화         |
| **CXL·분산 스토리지 패브릭** | RDMA는 별도 흐름제어 사용하나 WAN 구간은 BBR 연계 가능     |
| **SRv6·SD-WAN**     | 경로별 BtlBw 측정으로 트래픽 엔지니어링 고도화             |

***

**(제언)** "BBR은 '패킷을 잃어야만 혼잡을 아는' 반세기 가까운 손실 기반 패러다임을 '있는 그대로의 네트워크 상태를 실시간 측정한다'는 모델 기반 접근으로 전환한 혁신입니다. **앞서 다룬 QUIC가 BBR을 기본 혼잡제어로 채택해 HTTP/3의 저지연을 실현하고, 앞서 다룬 3GPP NTN 위성 통신처럼 지연이 크고 손실이 잦은 환경에서 BBR의 진가가 발휘되며, BBR과 CUBIC이 혼재할 때의 공정성 문제를 BBRv2·v3가 지속 개선하고 있으므로, 대규모 CDN·클라우드 트래픽 엔지니어링 설계 시 트래픽 특성(장거리·무선·혼재 환경)에 따라 혼잡제어 알고리즘을 선택하고 지속적으로 버전 업그레이드를 추적하는 것이 네트워크 성능 최적화의 핵심 전략입니다.**"
